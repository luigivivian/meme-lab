---
phase: 06-frontend-route-protection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - memelab/src/app/(app)/layout.tsx
  - memelab/src/app/login/page.tsx
  - memelab/src/app/register/page.tsx
autonomous: false
requirements:
  - FAUTH-03

must_haves:
  truths:
    - "Visiting /dashboard without a stored token redirects to /login"
    - "Visiting /login with a valid stored token redirects to /dashboard"
    - "Visiting /register with a valid stored token redirects to /dashboard"
    - "A full-screen spinner shows while auth state is loading (no sidebar flash)"
  artifacts:
    - path: "memelab/src/app/(app)/layout.tsx"
      provides: "Auth guard wrapping Shell — redirects unauthenticated users to /login"
      contains: "useAuth"
    - path: "memelab/src/app/login/page.tsx"
      provides: "Authenticated-user redirect to /dashboard"
      contains: "router.push"
    - path: "memelab/src/app/register/page.tsx"
      provides: "Authenticated-user redirect to /dashboard"
      contains: "router.push"
  key_links:
    - from: "memelab/src/app/(app)/layout.tsx"
      to: "memelab/src/contexts/auth-context.tsx"
      via: "useAuth() hook providing isAuthenticated and isLoading"
      pattern: "useAuth\\(\\)"
    - from: "memelab/src/app/(app)/layout.tsx"
      to: "/login"
      via: "router.push('/login') when not authenticated"
      pattern: "router\\.push.*login"
    - from: "memelab/src/app/login/page.tsx"
      to: "/dashboard"
      via: "router.push('/dashboard') when already authenticated"
      pattern: "router\\.push.*dashboard"
---

<objective>
Protect all (app) group routes with a client-side auth guard and redirect authenticated users away from login/register pages.

Purpose: Implements FAUTH-03 — unauthenticated visitors are redirected to /login before seeing any dashboard content. Authenticated visitors on /login or /register are sent to /dashboard.
Output: Three modified files implementing client-side route protection using the existing AuthContext.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-frontend-route-protection/06-CONTEXT.md

<interfaces>
<!-- Key types and contracts from AuthContext that both tasks depend on. -->

From memelab/src/contexts/auth-context.tsx:
```typescript
interface AuthContextType {
  user: { email: string; role: string } | null;
  isAuthenticated: boolean;
  isLoading: boolean;
  login: (email: string, password: string) => Promise<void>;
  register: (email: string, password: string) => Promise<void>;
  logout: () => Promise<void>;
}

export function useAuth(): AuthContextType;
```

Current memelab/src/app/(app)/layout.tsx (entire file):
```typescript
"use client";

import { Shell } from "@/components/layout/shell";

export default function AppLayout({ children }: { children: React.ReactNode }) {
  return <Shell>{children}</Shell>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add auth guard and loading spinner to (app) layout</name>
  <files>memelab/src/app/(app)/layout.tsx</files>
  <read_first>
    - memelab/src/app/(app)/layout.tsx (current state — simple Shell wrapper)
    - memelab/src/contexts/auth-context.tsx (useAuth hook providing isAuthenticated, isLoading)
    - memelab/src/components/layout/shell.tsx (Shell component to understand wrapper structure)
  </read_first>
  <action>
Modify `memelab/src/app/(app)/layout.tsx` to add a client-side auth guard per D-01 and D-03.

The file must:
1. Import `useAuth` from `@/contexts/auth-context`
2. Import `useRouter` from `next/navigation`
3. Import `useEffect` from `react`
4. Call `const { isAuthenticated, isLoading } = useAuth()`
5. Call `const router = useRouter()`
6. Add a `useEffect` that runs when `isAuthenticated` or `isLoading` changes:
   - If `!isLoading && !isAuthenticated`, call `router.push("/login")`
7. If `isLoading` is true, return a full-screen spinner instead of Shell:
   - A `div` with classes `min-h-screen flex items-center justify-center` and inline style `backgroundColor: "#09090b"` (per D-05)
   - Inside it, a `div` with classes `h-8 w-8 rounded-full border-4 border-t-transparent animate-spin` and inline style `borderColor: "#7C3AED", borderTopColor: "transparent"` (purple spinner per D-05)
8. If `!isAuthenticated` (and not loading), return `null` to prevent any flash of the Shell/dashboard content while the redirect fires
9. If `isAuthenticated`, return `<Shell>{children}</Shell>` as before

Do NOT use Edge Middleware. Do NOT add any text to the spinner screen (per D-05). Use `router.push()` not `window.location.href` (per established pattern in CONTEXT.md).
  </action>
  <verify>
    <automated>cd memelab && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - layout.tsx contains `import { useAuth } from "@/contexts/auth-context"`
    - layout.tsx contains `import { useRouter } from "next/navigation"`
    - layout.tsx contains `const { isAuthenticated, isLoading } = useAuth()`
    - layout.tsx contains `router.push("/login")`
    - layout.tsx contains `#09090b` (dark background color for spinner)
    - layout.tsx contains `#7C3AED` (purple spinner color)
    - layout.tsx contains `animate-spin` (spinner animation class)
    - layout.tsx contains `<Shell>{children}</Shell>` (preserved for authenticated users)
    - layout.tsx does NOT contain `middleware` or `NextResponse`
    - TypeScript compilation succeeds with zero errors
  </acceptance_criteria>
  <done>The (app) layout shows a dark full-screen spinner while auth loads, redirects to /login if unauthenticated, and renders Shell normally if authenticated.</done>
</task>

<task type="auto">
  <name>Task 2: Add authenticated-user redirect to login and register pages</name>
  <files>memelab/src/app/login/page.tsx, memelab/src/app/register/page.tsx</files>
  <read_first>
    - memelab/src/app/login/page.tsx (current login page — no auth redirect yet)
    - memelab/src/app/register/page.tsx (current register page — no auth redirect yet)
    - memelab/src/contexts/auth-context.tsx (useAuth hook)
  </read_first>
  <action>
Modify both login and register pages to redirect authenticated users to /dashboard per D-06.

**For `memelab/src/app/login/page.tsx`:**
1. Add imports: `useRouter` from `next/navigation`, `useEffect` from `react`
2. Destructure `isAuthenticated` and `isLoading` alongside existing `login` from `useAuth()`:
   `const { login, isAuthenticated, isLoading } = useAuth()`
3. Add `const router = useRouter()`
4. Add a `useEffect` that runs when `isAuthenticated` or `isLoading` changes:
   - If `!isLoading && isAuthenticated`, call `router.push("/dashboard")`
5. If `isLoading`, return the same full-screen spinner as the layout (dark bg #09090b, purple spinner #7C3AED, same classes)
6. If `isAuthenticated` (and not loading), return `null` to prevent flash while redirect fires
7. Otherwise render the existing login form as-is (no changes to form logic)

**For `memelab/src/app/register/page.tsx`:**
Apply the exact same pattern as login:
1. Add imports: `useRouter` from `next/navigation`, `useEffect` from `react`
2. Destructure `isAuthenticated` and `isLoading` alongside existing `register` from `useAuth()`:
   `const { register, isAuthenticated, isLoading } = useAuth()`
3. Add `const router = useRouter()`
4. Add the same `useEffect` redirect logic
5. Same loading spinner and authenticated null-return
6. Otherwise render the existing register form as-is

Do NOT remove any existing form logic, validation, or error handling. Only add the redirect behavior at the top of the component.
  </action>
  <verify>
    <automated>cd memelab && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - login/page.tsx contains `import { useRouter } from "next/navigation"`
    - login/page.tsx contains `const { login, isAuthenticated, isLoading } = useAuth()`
    - login/page.tsx contains `router.push("/dashboard")`
    - login/page.tsx contains `#09090b` (loading spinner background)
    - register/page.tsx contains `import { useRouter } from "next/navigation"`
    - register/page.tsx contains `const { register, isAuthenticated, isLoading } = useAuth()`
    - register/page.tsx contains `router.push("/dashboard")`
    - register/page.tsx contains `#09090b` (loading spinner background)
    - Both files still contain their existing form validation (e.g., login has `validate()`, register has `confirmPassword`)
    - TypeScript compilation succeeds with zero errors
  </acceptance_criteria>
  <done>Login and register pages redirect to /dashboard when user is already authenticated, and show a spinner while auth state loads.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Client-side route protection for all dashboard pages, plus authenticated-user redirect on login/register pages. Three files modified using the existing AuthContext.</what-built>
  <how-to-verify>
    1. Start the dev server: `cd memelab && npm run dev`
    2. Open browser to http://localhost:3000/dashboard (without being logged in)
       - Expected: Brief spinner, then redirect to /login
    3. Open http://localhost:3000/login
       - Expected: Login form renders normally
    4. Log in with valid credentials
       - Expected: Redirect to /dashboard, dashboard renders with Shell
    5. While logged in, navigate to http://localhost:3000/login
       - Expected: Brief spinner, then redirect to /dashboard
    6. While logged in, navigate to http://localhost:3000/register
       - Expected: Brief spinner, then redirect to /dashboard
    7. Click logout in the dashboard
       - Expected: Redirect to /login, cannot access /dashboard anymore
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- TypeScript compilation: `cd memelab && npx tsc --noEmit` exits 0
- All three files import and use `useAuth()` from auth-context
- (app)/layout.tsx guards all dashboard routes behind isAuthenticated check
- login/page.tsx and register/page.tsx redirect authenticated users to /dashboard
- No Edge Middleware files exist (no middleware.ts in memelab/src/)
</verification>

<success_criteria>
1. Unauthenticated user visiting any (app) route (dashboard, agents, pipeline, etc.) sees a spinner then gets redirected to /login
2. Authenticated user visiting /login or /register gets redirected to /dashboard
3. All redirects use client-side router.push() (per D-01, D-02)
4. Loading state shows full-screen dark spinner with no sidebar flash (per D-05)
5. TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-frontend-route-protection/06-01-SUMMARY.md`
</output>
