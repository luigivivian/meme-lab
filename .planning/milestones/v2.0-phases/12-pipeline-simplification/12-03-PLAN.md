---
phase: 12-pipeline-simplification
plan: 03
type: execute
wave: 3
depends_on: ["12-01", "12-02"]
files_modified: []
autonomous: false
requirements: [PIPE-01, PIPE-02, PIPE-03, PIPE-04]

must_haves:
  truths:
    - "Full manual pipeline flow works end-to-end: form -> run -> results -> approve/reject"
    - "Composed meme images are visually correct (background + phrase text rendered by Pillow)"
    - "No Gemini Image API calls were made during the run"
    - "Approved/rejected statuses persist across page refresh"
  artifacts: []
  key_links: []
---

<objective>
End-to-end integration verification of the manual pipeline flow. Human verifies the full visual and functional experience.

Purpose: Catches visual issues, UX problems, and integration gaps that automated tests cannot cover.
Output: Verification sign-off or issues list for fix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-pipeline-simplification/12-CONTEXT.md
@.planning/phases/12-pipeline-simplification/12-UI-SPEC.md
@.planning/phases/12-pipeline-simplification/12-01-SUMMARY.md
@.planning/phases/12-pipeline-simplification/12-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run Alembic migration and start servers</name>
  <files></files>
  <read_first>
    src/database/migrations/versions/009_add_approval_status.py
  </read_first>
  <action>
    1. Run the Alembic migration to add approval_status column:
       `cd C:/Users/VIP/testeDev/clip-flow && python -m alembic upgrade head`
    2. Start the backend API server in background:
       `python -m uvicorn src.api.main:app --host 127.0.0.1 --port 8000`
       (verify it starts without import errors)
    3. Start the frontend dev server in background:
       `cd memelab && npm run dev`
       (verify it compiles without errors)
    4. Verify the new endpoints are registered by checking Swagger at http://127.0.0.1:8000/docs — look for `/pipeline/manual-run`, `/pipeline/content/{package_id}/approve`, `/pipeline/themes`, `/pipeline/backgrounds/{character_slug}`.
    5. Run a quick smoke test via curl:
       `curl -s http://127.0.0.1:8000/docs | head -5` to confirm API is up
       `curl -s http://127.0.0.1:8000/api/pipeline/themes | python -m json.tool | head -10` to confirm themes endpoint returns data with colors
  </action>
  <verify>
    <automated>cd C:/Users/VIP/testeDev/clip-flow && python -m alembic upgrade head 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Alembic migration runs without error
    - Backend starts without import errors
    - Swagger at /docs lists manual-run endpoint
    - curl to /api/pipeline/themes returns JSON with "colors" arrays
  </acceptance_criteria>
  <done>Migration applied, both servers running, all endpoints visible in Swagger, themes endpoint returns colors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify full manual pipeline flow</name>
  <files></files>
  <action>
    Present the user with the verification checklist below. This is a human-verify checkpoint — Claude does NOT perform these steps, the user does.

    Complete manual pipeline mode has been built: form with input mode tabs (topic/phrase), theme + background selection (solid colors with per-theme palettes, or image backgrounds with upload), meme count, L5 toggle, results grid with approve/reject per meme, bulk actions, and un-reject via badge click.
  </action>
  <verify>
    <automated>echo "CHECKPOINT: Awaiting human verification"</automated>
  </verify>
  <acceptance_criteria>
    - User confirms the full manual pipeline flow works end-to-end
    - User confirms composed images look correct visually
    - User confirms approve/reject/un-reject work
  </acceptance_criteria>
  <done>User typed "approved" confirming the full flow works, or provided specific issues for fix.</done>
  <how-to-verify>
    1. Open http://localhost:3000/pipeline in browser (login if needed)
    2. **Form verification:**
       - Verify two tabs: "Gerar do tema" and "Usar minha frase"
       - Click "Usar minha frase" — verify textarea appears with placeholder
       - Click "Gerar do tema" — verify topic input appears
       - Verify "Cor solida" and "Imagem" background type cards
       - Select a theme — verify color swatches appear (3-5 circles)
       - Click a swatch — verify violet ring selection indicator
       - Switch to "Imagem" — verify thumbnail strip loads
       - Try uploading a JPG via the "+" button
       - Verify meme count input (default 3) and L5 checkbox (default on)
    3. **Run test (phrase mode):**
       - Switch to "Usar minha frase"
       - Enter 2 phrases (one per line)
       - Select a solid color background
       - Set count to 2
       - Click "Gerar Memes"
       - Verify progress bar appears and CTA shows "Gerando..."
       - Wait for completion — verify results grid appears
    4. **Results verification:**
       - Verify memes show in grid with 4:5 aspect images
       - Verify phrase text visible on composed image
       - Verify background is the solid color you chose
       - Click approve on one meme — verify emerald border + "aprovado" badge
       - Click reject on another — verify opacity + "rejeitado" badge
       - Click "rejeitado" badge — verify it un-rejects (back to pending)
       - Click "Aprovar Todos" — verify all get approved status
    5. **Run test (topic mode):**
       - Switch to "Gerar do tema"
       - Enter topic "cafe"
       - Set count to 3
       - Click "Gerar Memes"
       - Wait for completion — verify 3 memes generated with Gemini text phrases
    6. **Persistence check:**
       - Refresh the page
       - Verify approved/rejected statuses persist (check via gallery or re-run)
  </how-to-verify>
  <resume-signal>Type "approved" if full flow works, or describe specific issues</resume-signal>
</task>

</tasks>

<verification>
- Alembic migration applied successfully
- Both servers start without errors
- Full manual pipeline flow works end-to-end
- Visual composition is correct (background + text)
- Approve/reject/un-reject work with optimistic UI
- No Gemini Image API calls in logs during runs
</verification>

<success_criteria>
Human verifies that the complete Phase 12 feature works: manual pipeline mode with theme/background selection, Pillow composition, approve/reject workflow. All PIPE-01 through PIPE-04 requirements met.
</success_criteria>

<output>
After completion, create `.planning/phases/12-pipeline-simplification/12-03-SUMMARY.md`
</output>
