---
phase: 12.1-viral-content-engine-trend-agents-content-quality-overhaul
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pipeline/agents/hackernews.py
  - src/pipeline/agents/lemmy_communities.py
  - src/pipeline/agents/rss_feeds.py
  - src/pipeline/agents/reddit_memes.py
  - src/pipeline/agents/youtube_rss.py
  - src/pipeline/agents/gemini_web_trends.py
  - src/pipeline/agents/bluesky_trends.py
  - src/pipeline/async_orchestrator.py
  - config.py
autonomous: true
requirements: [VIRAL-01, VIRAL-02, VIRAL-03, VIRAL-04, VIRAL-05, VIRAL-06, VIRAL-07]

must_haves:
  truths:
    - "HackerNews and Lemmy agents are deleted and no longer registered in the orchestrator"
    - "RSSFeedAgent only fetches Sensacionalista + cannabis feeds (no Reddit RSS duplicates)"
    - "RedditMemesAgent scores BR subs at 0.6 base and English subs at 0.3 base"
    - "YouTubeRSSAgent includes Felipe Neto, Whindersson, Casimiro, Gaules, Coreano Doido channels"
    - "GeminiWebTrendsAgent requests 25 topics per fetch instead of 15"
    - "BlueSkyAgent uses improved BR humor keywords"
  artifacts:
    - path: "src/pipeline/agents/rss_feeds.py"
      provides: "Simplified RSS agent with only Sensacionalista + cannabis feeds"
      contains: "sensacionalista"
    - path: "src/pipeline/agents/reddit_memes.py"
      provides: "BR-first Reddit scoring"
      contains: "0.6"
    - path: "src/pipeline/agents/youtube_rss.py"
      provides: "Expanded YouTube channels"
      contains: "Felipe Neto"
    - path: "src/pipeline/async_orchestrator.py"
      provides: "Updated agent registration without HN/Lemmy"
  key_links:
    - from: "src/pipeline/async_orchestrator.py"
      to: "agent modules"
      via: "import and instantiation in __init__"
      pattern: "HackerNewsAgent|LemmyCommunitiesAgent"
    - from: "config.py"
      to: "agent modules"
      via: "config constants"
      pattern: "GEMINI_TRENDS_MAX_TOPICS|BLUESKY_MAX_POSTS"
---

<objective>
Clean up low-quality trend agents, improve remaining agents for Brazilian meme relevance, and update orchestrator registration.

Purpose: The current agent layer produces irrelevant content (HackerNews tech news, Lemmy duplicates, RSS Reddit overlaps). This plan removes noise sources and improves the signal-to-noise ratio of every remaining agent to focus on Brazilian humor and viral content.

Output: 7 agent files updated/deleted, orchestrator updated, config updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12.1-viral-content-engine-trend-agents-content-quality-overhaul/12.1-CONTEXT.md

@src/pipeline/async_orchestrator.py
@src/pipeline/agents/hackernews.py
@src/pipeline/agents/lemmy_communities.py
@src/pipeline/agents/rss_feeds.py
@src/pipeline/agents/reddit_memes.py
@src/pipeline/agents/youtube_rss.py
@src/pipeline/agents/gemini_web_trends.py
@src/pipeline/agents/bluesky_trends.py
@src/pipeline/agents/async_base.py
@src/pipeline/models_v2.py
@config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove HN/Lemmy agents, simplify RSS, improve Reddit/BlueSky/YouTube/Gemini agents</name>
  <files>
    src/pipeline/agents/hackernews.py,
    src/pipeline/agents/lemmy_communities.py,
    src/pipeline/agents/rss_feeds.py,
    src/pipeline/agents/reddit_memes.py,
    src/pipeline/agents/youtube_rss.py,
    src/pipeline/agents/gemini_web_trends.py,
    src/pipeline/agents/bluesky_trends.py,
    config.py
  </files>
  <read_first>
    src/pipeline/agents/hackernews.py,
    src/pipeline/agents/lemmy_communities.py,
    src/pipeline/agents/rss_feeds.py,
    src/pipeline/agents/reddit_memes.py,
    src/pipeline/agents/youtube_rss.py,
    src/pipeline/agents/gemini_web_trends.py,
    src/pipeline/agents/bluesky_trends.py,
    src/pipeline/models_v2.py,
    config.py
  </read_first>
  <action>
    **Per D-01: DELETE `src/pipeline/agents/hackernews.py`** — remove the entire file.

    **Per D-02: DELETE `src/pipeline/agents/lemmy_communities.py`** — remove the entire file.

    **Per D-03: SIMPLIFY `src/pipeline/agents/rss_feeds.py`** — replace `DEFAULT_FEEDS` list to contain only:
    ```python
    DEFAULT_FEEDS = [
        "https://www.sensacionalista.com.br/feed/",
        "https://www.smokebuddies.com.br/feed/",
        "https://maryjuana.com.br/feed/",
    ]
    ```
    Remove the Reddit RSS feeds (desabafos, antitrampo) since those are already covered by RedditMemesAgent and BrazilViralRSSAgent.

    **Per D-04: IMPROVE `src/pipeline/agents/reddit_memes.py`** — implement BR-first scoring:
    1. Replace `DEFAULT_SUBREDDITS` list with a list of tuples `(subreddit_name, base_score)`:
       ```python
       DEFAULT_SUBREDDITS = [
           # BR subs — high relevance
           ("DiretoDoZapZap", 0.6),
           ("tiodopave", 0.6),
           ("desabafos", 0.6),
           ("antitrampo", 0.6),
           # Cannabis BR
           ("maconha", 0.6),
           ("cultivonha", 0.6),
           # English/mixed subs — lower relevance
           ("trees", 0.3),
           ("microgrowery", 0.3),
       ]
       ```
    2. Remove `meirl` if present in config.py `PIPELINE_REDDIT_SUBREDDITS`.
    3. Update `_fetch_subreddit` to accept and use the base_score instead of hardcoded `0.4`.
    4. Update `fetch()` to iterate `(subreddit, base_score)` tuples.
    5. Keep using `TrendItem` and `BaseSourceAgent` (sync agent, wrapped by SyncAgentAdapter).

    **Per D-05: EXPAND `src/pipeline/agents/youtube_rss.py`** — add 5 major Brazilian comedy/entertainment channels to `_CHANNELS`:
    ```python
    # New channels to add (verify channel_ids before adding):
    ("UC254GjMuz2eFYfMEyEH0ZTQ", "Felipe Neto", 0.75, "comedia"),
    ("UC5Lkmon4P8bmjTOLeEZ3cqA", "Whindersson Nunes", 0.75, "comedia"),
    ("UCZbgt7GZQHM__B7PyhBaABQ", "Casimiro", 0.78, "entretenimento"),
    ("UCyMr-FAsKUba4ALoXCFDG5Q", "Gaules", 0.65, "gaming"),
    ("UCyZ_tOP7NYpnIziIVXSaYYw", "Coreano Doido", 0.72, "comedia"),
    ```
    NOTE: Channel IDs above should be verified via YouTube RSS before adding. If a channel_id returns empty feed from `https://www.youtube.com/feeds/videos.xml?channel_id={id}`, skip it and log a warning.

    **Per D-06: EXPAND `src/pipeline/agents/gemini_web_trends.py`** — update `_USER_PROMPT` to request 25 topics instead of reading from config. Also update `config.py` constant:
    - In `config.py`: change `GEMINI_TRENDS_MAX_TOPICS = 15` to `GEMINI_TRENDS_MAX_TOPICS = 25`
    - The `_USER_PROMPT` in gemini_web_trends.py already reads from `GEMINI_TRENDS_MAX_TOPICS`, so it will automatically pick up the change.

    **Per D-07: IMPROVE `src/pipeline/agents/bluesky_trends.py`** — replace `_KEYWORDS_BR` list with more specific BR humor terms:
    ```python
    _KEYWORDS_BR = [
        "humor br",
        "zoeira",
        "piada do dia",
        "memes brasileiros",
        "tio do pave",
        "meme viral brasil",
        "humor brasileiro",
        "zoeira brasileira",
        "engraçado demais",
        "kkkkk meme",
    ]
    ```

    **Config cleanup in `config.py`:**
    - Remove `HACKERNEWS_MAX_STORIES` constant.
    - Remove `LEMMY_MAX_POSTS` constant.
    - Remove `meirl` from `PIPELINE_REDDIT_SUBREDDITS`.
    - Remove Reddit RSS feeds from `PIPELINE_RSS_FEEDS` (keep only `https://www.sensacionalista.com.br/feed/`).
    - Update `GEMINI_TRENDS_MAX_TOPICS = 25`.
  </action>
  <verify>
    <automated>python -c "
# Verify deleted files don't exist
import os, sys
for f in ['src/pipeline/agents/hackernews.py', 'src/pipeline/agents/lemmy_communities.py']:
    if os.path.exists(f):
        print(f'FAIL: {f} still exists'); sys.exit(1)

# Verify RSS feeds simplified
from src.pipeline.agents.rss_feeds import DEFAULT_FEEDS
assert 'reddit.com' not in str(DEFAULT_FEEDS), f'Reddit RSS still in feeds: {DEFAULT_FEEDS}'
assert 'sensacionalista' in str(DEFAULT_FEEDS).lower(), 'Sensacionalista missing'

# Verify Reddit BR-first scoring
from src.pipeline.agents.reddit_memes import DEFAULT_SUBREDDITS
assert any(0.6 == s[1] for s in DEFAULT_SUBREDDITS if s[0] == 'DiretoDoZapZap'), 'BR sub should have 0.6 score'
assert any(0.3 == s[1] for s in DEFAULT_SUBREDDITS if s[0] == 'trees'), 'English sub should have 0.3 score'

# Verify YouTube expanded
from src.pipeline.agents.youtube_rss import _CHANNELS
channel_names = [c[1] for c in _CHANNELS]
assert 'Felipe Neto' in channel_names, 'Felipe Neto missing'

# Verify Gemini topics expanded
from config import GEMINI_TRENDS_MAX_TOPICS
assert GEMINI_TRENDS_MAX_TOPICS == 25, f'Expected 25, got {GEMINI_TRENDS_MAX_TOPICS}'

# Verify BlueSky keywords updated
from src.pipeline.agents.bluesky_trends import _KEYWORDS_BR
assert 'humor br' in _KEYWORDS_BR or 'zoeira' in _KEYWORDS_BR, f'BR keywords not updated: {_KEYWORDS_BR}'

print('All agent checks passed')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - `src/pipeline/agents/hackernews.py` does NOT exist on disk
    - `src/pipeline/agents/lemmy_communities.py` does NOT exist on disk
    - `grep -c "reddit.com" src/pipeline/agents/rss_feeds.py` returns 0
    - `grep -c "sensacionalista" src/pipeline/agents/rss_feeds.py` returns at least 1
    - `grep -c "0.6" src/pipeline/agents/reddit_memes.py` returns at least 4 (4 BR subs)
    - `grep -c "0.3" src/pipeline/agents/reddit_memes.py` returns at least 2 (2 English subs)
    - `grep -c "Felipe Neto" src/pipeline/agents/youtube_rss.py` returns 1
    - `grep -c "GEMINI_TRENDS_MAX_TOPICS = 25" config.py` returns 1
    - `grep -c "humor br\|zoeira\|tio do pave" src/pipeline/agents/bluesky_trends.py` returns at least 2
    - `grep -c "meirl" config.py` returns 0
    - `grep -c "HACKERNEWS_MAX_STORIES" config.py` returns 0
    - `grep -c "LEMMY_MAX_POSTS" config.py` returns 0
  </acceptance_criteria>
  <done>HackerNews and Lemmy agents deleted. RSS simplified to Sensacionalista + cannabis only. Reddit uses BR-first scoring (0.6 BR, 0.3 English). YouTube expanded with 5 Brazilian comedy channels. Gemini requests 25 topics. BlueSky uses specific BR humor keywords. Config cleaned of removed agent constants.</done>
</task>

<task type="auto">
  <name>Task 2: Update orchestrator agent registration and remove dead imports</name>
  <files>
    src/pipeline/async_orchestrator.py,
    src/pipeline/models_v2.py
  </files>
  <read_first>
    src/pipeline/async_orchestrator.py,
    src/pipeline/models_v2.py,
    config.py
  </read_first>
  <action>
    **Update `src/pipeline/async_orchestrator.py`:**
    1. Remove import lines:
       ```python
       from src.pipeline.agents.hackernews import HackerNewsAgent
       from src.pipeline.agents.lemmy_communities import LemmyCommunitiesAgent
       ```
    2. Remove `HackerNewsAgent()` and `LemmyCommunitiesAgent()` from the `async_agents_list` in `__init__` (around line 98-103).
    3. Remove `HACKERNEWS_MAX_STORIES` and `LEMMY_MAX_POSTS` if imported from config (they are not currently imported in orchestrator, but verify).
    4. The orchestrator currently passes `PIPELINE_REDDIT_SUBREDDITS` to `RedditMemesAgent(subreddits=...)`. Since RedditMemesAgent now uses tuples `(name, score)` internally via `DEFAULT_SUBREDDITS`, update the orchestrator to NOT pass the config subreddits — let the agent use its own defaults:
       - Change `RedditMemesAgent(subreddits=PIPELINE_REDDIT_SUBREDDITS)` to `RedditMemesAgent()`
       - Remove `PIPELINE_REDDIT_SUBREDDITS` from the config import line at top of file.
    5. Similarly, `RSSFeedAgent(feeds=PIPELINE_RSS_FEEDS)` should become `RSSFeedAgent()` to use the agent's own cleaned-up `DEFAULT_FEEDS`.
       - Remove `PIPELINE_RSS_FEEDS` from the config import line.

    **Update `src/pipeline/models_v2.py`:**
    1. Remove `HACKERNEWS = "hackernews"` from the `TrendSource` enum.
    2. Remove `LEMMY = "lemmy"` from the `TrendSource` enum.
    (These are dead values now that their agents are deleted.)
  </action>
  <verify>
    <automated>python -c "
# Verify orchestrator imports clean
import ast
with open('src/pipeline/async_orchestrator.py') as f:
    source = f.read()
assert 'HackerNewsAgent' not in source, 'HackerNewsAgent still referenced'
assert 'LemmyCommunitiesAgent' not in source, 'LemmyCommunitiesAgent still referenced'
assert 'PIPELINE_REDDIT_SUBREDDITS' not in source, 'PIPELINE_REDDIT_SUBREDDITS still referenced'
assert 'PIPELINE_RSS_FEEDS' not in source, 'PIPELINE_RSS_FEEDS still referenced'

# Verify models_v2 TrendSource cleaned
with open('src/pipeline/models_v2.py') as f:
    source2 = f.read()
assert 'HACKERNEWS' not in source2, 'HACKERNEWS still in TrendSource'
assert 'LEMMY' not in source2, 'LEMMY still in TrendSource'

# Verify orchestrator can be imported (syntax check)
import importlib, sys
if 'src.pipeline.async_orchestrator' in sys.modules:
    del sys.modules['src.pipeline.async_orchestrator']

print('Orchestrator cleanup checks passed')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "HackerNewsAgent" src/pipeline/async_orchestrator.py` returns 0
    - `grep -c "LemmyCommunitiesAgent" src/pipeline/async_orchestrator.py` returns 0
    - `grep -c "PIPELINE_REDDIT_SUBREDDITS" src/pipeline/async_orchestrator.py` returns 0
    - `grep -c "PIPELINE_RSS_FEEDS" src/pipeline/async_orchestrator.py` returns 0
    - `grep -c "HACKERNEWS" src/pipeline/models_v2.py` returns 0
    - `grep -c "LEMMY" src/pipeline/models_v2.py` returns 0
    - `RedditMemesAgent()` is instantiated without arguments in orchestrator
    - `RSSFeedAgent()` is instantiated without arguments in orchestrator
  </acceptance_criteria>
  <done>Orchestrator no longer imports or registers HackerNews/Lemmy agents. Reddit and RSS agents use their own curated defaults instead of config overrides. TrendSource enum cleaned of dead values.</done>
</task>

</tasks>

<verification>
- No import errors when loading `src.pipeline.async_orchestrator`
- Agent count should be 7 (GoogleTrends, Reddit, RSS, YouTube, BrazilViralRSS, BlueSky, GeminiWebTrends) + stubs
- No references to HackerNews or Lemmy in any pipeline file
</verification>

<success_criteria>
- 2 agent files deleted (hackernews.py, lemmy_communities.py)
- 5 agent files improved (rss_feeds, reddit_memes, youtube_rss, gemini_web_trends, bluesky_trends)
- Orchestrator cleaned of dead agent references
- Config cleaned of dead constants
- All remaining agents focus on Brazilian humor content
</success_criteria>

<output>
After completion, create `.planning/phases/12.1-viral-content-engine-trend-agents-content-quality-overhaul/12.1-01-SUMMARY.md`
</output>
