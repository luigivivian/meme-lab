---
phase: 12.1-viral-content-engine-trend-agents-content-quality-overhaul
verified: 2026-03-26T18:00:00Z
status: passed
score: 11/11 must-haves verified
re_verification: false
gaps: []
human_verification:
  - test: "Run a full pipeline cycle and confirm 10-15 WorkOrders are produced"
    expected: "Pipeline drains 30 trends, curator emits 10+ WorkOrders after meme_potential filter"
    why_human: "Requires live Gemini API call and running orchestrator — cannot verify offline"
  - test: "Trigger a coherence mismatch (e.g., feed 'cafe' topic with 'confronto' theme) and verify remap occurs"
    expected: "Log shows 'Coherence mismatch' then 'Remapped: confronto -> cafe'"
    why_human: "LLM-dependent behavior; requires live pipeline run to observe remap logs"
---

# Phase 12.1: Viral Content Engine — Trend Agents & Content Quality Overhaul Verification Report

**Phase Goal:** Overhaul the trend agent layer (L1) and curator layer (L3) to produce content that is consistently relevant, timely, and viral-ready for Brazilian meme audiences. Remove low-quality sources, fix scoring bugs, add engagement-based ranking, improve theme-to-topic alignment via LLM, and boost multi-source trends.
**Verified:** 2026-03-26T18:00:00Z
**Status:** PASSED
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| #  | Truth                                                                                               | Status     | Evidence                                                                                              |
|----|-----------------------------------------------------------------------------------------------------|------------|-------------------------------------------------------------------------------------------------------|
| 1  | HackerNews and Lemmy agents are deleted and no longer registered in the orchestrator                | VERIFIED   | Both files absent on disk; no import/instantiation in async_orchestrator.py; HACKERNEWS/LEMMY removed from TrendSource enum |
| 2  | RSSFeedAgent only fetches Sensacionalista + cannabis feeds (no Reddit RSS duplicates)               | VERIFIED   | `DEFAULT_FEEDS` = 3 entries: sensacionalista + smokebuddies + maryjuana; no reddit.com reference     |
| 3  | RedditMemesAgent uses 0.6 base for BR subs and 0.3 base for English subs                           | VERIFIED   | `DEFAULT_SUBREDDITS` tuple list confirmed: DiretoDoZapZap/tiodopave/desabafos/antitrampo/maconha/cultivonha=0.6; trees/microgrowery=0.3 |
| 4  | YouTubeRSSAgent includes Felipe Neto, Whindersson, Casimiro, Gaules, Coreano Doido                 | VERIFIED   | All 5 channels present in `_CHANNELS` with correct channel_ids and base scores                       |
| 5  | GeminiWebTrendsAgent requests 25 topics per fetch                                                   | VERIFIED   | `GEMINI_TRENDS_MAX_TOPICS = 25` in config.py; `_USER_PROMPT` reads from this constant                |
| 6  | BlueSkyAgent uses improved BR humor keywords                                                        | VERIFIED   | `_KEYWORDS_BR` = 10 specific terms: "humor br", "zoeira", "piada do dia", "tio do pave", etc.        |
| 7  | Google Trends traffic parsing uses regex to handle K/M/B suffixes                                  | VERIFIED   | `_parse_traffic()` function with `re.search(r'(\d+\.?\d*)\s*([KMBkmb]?)')` and multipliers dict      |
| 8  | Trend scores decay exponentially based on age (e^(-age_hours/24))                                  | VERIFIED   | `_apply_temporal_decay()` in aggregator.py uses `math.exp(-age_hours / 24.0)`                        |
| 9  | Trends appearing in 2+ sources receive multi-source boost of 1 + 0.2*(sources-1), capped at 2.0x  | VERIFIED   | `source_counts` tracking and `min(1.0 + 0.2 * (sources-1), 2.0)` boost in aggregator.py             |
| 10 | Curator uses LLM to map trending topics to best visual theme, with KEYWORD_MAP as fallback          | VERIFIED   | `_llm_map_theme()` in curator.py calls generate(tier="lite"); `_match_situacao_diversa()` tries LLM -> KEYWORD_MAP -> random |
| 11 | Curator filters topics with meme_potential < 3 before emitting WorkOrders                          | VERIFIED   | `_selections_to_topics()` filters `s.get("meme_potential", 3) >= 3`; fallback preserves all if everything filtered |

**Score:** 11/11 truths verified

---

### Required Artifacts

| Artifact                                          | Provides                                        | Status     | Details                                                          |
|---------------------------------------------------|-------------------------------------------------|------------|------------------------------------------------------------------|
| `src/pipeline/agents/hackernews.py`               | DELETED                                         | VERIFIED   | File does not exist on disk                                      |
| `src/pipeline/agents/lemmy_communities.py`        | DELETED                                         | VERIFIED   | File does not exist on disk                                      |
| `src/pipeline/agents/rss_feeds.py`                | Simplified RSS agent                            | VERIFIED   | Contains "sensacionalista"; no reddit.com reference; 51 lines    |
| `src/pipeline/agents/reddit_memes.py`             | BR-first Reddit scoring                         | VERIFIED   | Contains "0.6" (6x), tuple-based DEFAULT_SUBREDDITS; 101 lines  |
| `src/pipeline/agents/youtube_rss.py`              | Expanded YouTube channels                       | VERIFIED   | Contains "Felipe Neto" and 4 other new BR channels; 12 total    |
| `src/pipeline/agents/bluesky_trends.py`           | Improved BR humor keywords                      | VERIFIED   | `_KEYWORDS_BR` list with 10 specific BR humor terms             |
| `src/pipeline/agents/gemini_web_trends.py`        | 25 topics per fetch                             | VERIFIED   | Reads `GEMINI_TRENDS_MAX_TOPICS` from config; value = 25        |
| `src/pipeline/async_orchestrator.py`              | Updated agent registration without HN/Lemmy     | VERIFIED   | No HackerNewsAgent/LemmyCommunitiesAgent import or instantiation; agents use own defaults |
| `src/pipeline/models_v2.py`                       | TrendSource enum without HACKERNEWS/LEMMY       | VERIFIED   | 11 enum values; HACKERNEWS and LEMMY are absent                 |
| `config.py`                                       | GEMINI_TRENDS_MAX_TOPICS=25; legacy constants   | VERIFIED   | GEMINI_TRENDS_MAX_TOPICS=25; HACKERNEWS_MAX_STORIES/LEMMY_MAX_POSTS/meirl absent |
| `src/pipeline/agents/google_trends.py`            | Regex traffic parsing with K/M/B               | VERIFIED   | `_parse_traffic()` uses `re.search`; multipliers dict; score thresholds 0.4-0.95 |
| `src/pipeline/processors/aggregator.py`           | Multi-source boost and temporal decay           | VERIFIED   | `import math`; `source_counts`; `min(1.0 + 0.2*(sources-1), 2.0)`; `_apply_temporal_decay` |
| `src/pipeline/curator.py`                         | LLM-based theme mapping and relevance filter    | VERIFIED   | `_llm_map_theme()` defined; `_theme_cache`; `count: int = 15`; `tier="lite"` |
| `src/pipeline/processors/analyzer.py`             | Meme potential scoring and filter               | VERIFIED   | `meme_potential` in ANALYZER_PROMPT and OLLAMA_SINGLE_PROMPT; filter in `_selections_to_topics()` |
| `src/pipeline/workers/phrase_worker.py`           | Phrase validation with retry                   | VERIFIED   | `_validate_phrase()` defined and called 3+ times; retry logic in both generate() and generate_with_scoring() |
| `src/pipeline/workers/generation_layer.py`        | Topic-image coherence check                    | VERIFIED   | `_check_coherence()` and `_remap_theme()` defined; coherence check before image generation |

---

### Key Link Verification

| From                                      | To                                      | Via                                      | Status     | Details                                                                        |
|-------------------------------------------|-----------------------------------------|------------------------------------------|------------|--------------------------------------------------------------------------------|
| `async_orchestrator.py`                   | agent modules                           | import + instantiation in `__init__`     | VERIFIED   | No HackerNewsAgent/LemmyCommunitiesAgent references; 7 core agents registered |
| `config.py`                               | `gemini_web_trends.py`                  | `GEMINI_TRENDS_MAX_TOPICS` constant      | VERIFIED   | Constant = 25; imported and used in `_USER_PROMPT`                             |
| `aggregator.py`                           | `models_v2.py` / `models.py`            | `TrendItem.fetched_at` for decay         | VERIFIED   | `trend.fetched_at` used in `_apply_temporal_decay()`                           |
| `aggregator.py`                           | source_counts dict                      | multi-source boost tracking              | VERIFIED   | `source_counts[key]` tracked per normalized title; boost applied post-dedup   |
| `curator.py`                              | `src/llm_client.py`                     | `generate(tier="lite")` in LLM mapping  | VERIFIED   | `from src.llm_client import generate`; `generate(..., tier="lite")` called    |
| `curator.py`                              | `src/pipeline/processors/analyzer.py`  | `self._analyzer.analyze(trend_items)`    | VERIFIED   | `asyncio.to_thread(self._analyzer.analyze, trend_items, count)` at line 193   |
| `async_orchestrator.py`                   | `curator.py`                            | `curator.curate()` with drain(max_items=30) | VERIFIED | `broker.drain(max_items=30)` + `curator_count = max(images_per_run*3, 15)`   |
| `phrase_worker.py`                        | `src/llm_client.py`                     | `generate(tier="lite")` in validation   | VERIFIED   | `_generate_with_prompt()` uses `generate(..., tier="lite")`                   |
| `generation_layer.py`                     | `curator.py`                            | `_remap_theme()` on coherence failure    | VERIFIED   | `_remap_theme()` defined inline; imports `_load_all_situacao_keys` from curator at runtime |

---

### Data-Flow Trace (Level 4)

| Artifact                          | Data Variable         | Source                                 | Produces Real Data | Status      |
|-----------------------------------|-----------------------|----------------------------------------|--------------------|-------------|
| `aggregator.py`                   | `source_counts`       | `all_trends` input list                | Yes                | FLOWING     |
| `aggregator.py`                   | `decay_factor`        | `trend.fetched_at` datetime field      | Yes                | FLOWING     |
| `curator.py`                      | `_theme_cache`        | `generate()` LLM call with `tier=lite` | Yes (+ cached)     | FLOWING     |
| `analyzer.py`                     | `meme_potential`      | LLM JSON response field                | Yes                | FLOWING     |
| `phrase_worker.py`                | `validated`           | `_validate_phrase()` filter on phrases | Yes                | FLOWING     |
| `generation_layer.py`             | `wo.situacao_key`     | `_remap_theme()` LLM call on mismatch  | Yes                | FLOWING     |

---

### Behavioral Spot-Checks

| Behavior                                          | Check                                                                | Result                                       | Status  |
|---------------------------------------------------|----------------------------------------------------------------------|----------------------------------------------|---------|
| hackernews.py deleted                             | `ls src/pipeline/agents/hackernews.py`                               | File not found                               | PASS    |
| lemmy_communities.py deleted                      | `ls src/pipeline/agents/lemmy_communities.py`                        | File not found                               | PASS    |
| Reddit BR sub has 0.6 base                        | grep "0.6" reddit_memes.py                                           | 6 matches (DiretoDoZapZap, tiodopave, desabafos, antitrampo, maconha, cultivonha) | PASS |
| Google Trends regex parser                        | grep "re.search" google_trends.py                                    | Match found in `_parse_traffic()`             | PASS    |
| Aggregator has exponential decay                  | grep "math.exp" aggregator.py                                        | Match found in `_apply_temporal_decay()`      | PASS    |
| Aggregator has multi-source boost                 | grep "source_counts" aggregator.py                                   | Match found; boost formula present            | PASS    |
| Curator uses LLM mapping with cache               | grep "_theme_cache\|_llm_map_theme" curator.py                       | Both defined and used                         | PASS    |
| Meme potential filter in analyzer                 | grep "meme_potential" analyzer.py                                    | In prompt + in `_selections_to_topics()` filter | PASS  |
| Phrase validation with retry in phrase_worker     | grep "_validate_phrase" phrase_worker.py                             | Definition + 4 call sites                     | PASS    |
| Coherence check in generation_layer               | grep "_check_coherence\|coherence" generation_layer.py               | Defined + called before image generation      | PASS    |
| Orchestrator drains 30 trends                     | grep "max_items=30" async_orchestrator.py                            | Match found at line 346                       | PASS    |
| Curator count formula present                     | grep "curator_count" async_orchestrator.py                           | `max(images_per_run * 3, 15)` at line 349     | PASS    |
| GEMINI_TRENDS_MAX_TOPICS = 25                     | grep in config.py                                                    | Confirmed at line 248                         | PASS    |
| HACKERNEWS_MAX_STORIES absent in config           | grep in config.py                                                    | No match                                      | PASS    |
| LEMMY_MAX_POSTS absent in config                  | grep in config.py                                                    | No match                                      | PASS    |
| meirl absent from config                          | grep in config.py                                                    | No match                                      | PASS    |

---

### Requirements Coverage

| Requirement | Source Plan | Description                                                                                     | Status       | Evidence                                                                 |
|-------------|-------------|-------------------------------------------------------------------------------------------------|--------------|--------------------------------------------------------------------------|
| VIRAL-01    | 12.1-01     | HackerNewsAgent removed from pipeline                                                           | SATISFIED    | hackernews.py deleted; no import in orchestrator                         |
| VIRAL-02    | 12.1-01     | LemmyCommunitiesAgent removed from pipeline                                                     | SATISFIED    | lemmy_communities.py deleted; no import in orchestrator                  |
| VIRAL-03    | 12.1-01     | RSSFeedAgent simplified to Sensacionalista + cannabis feeds only                                | SATISFIED    | DEFAULT_FEEDS = 3 entries, all BR, no reddit.com                         |
| VIRAL-04    | 12.1-01     | RedditMemesAgent uses BR-first scoring (BR subs 0.6, English subs 0.3)                         | SATISFIED    | Tuple-based DEFAULT_SUBREDDITS with 0.6/0.3 base scores                 |
| VIRAL-05    | 12.1-01     | YouTubeRSSAgent expanded with 5+ major Brazilian channels                                       | SATISFIED    | 5 new channels: Felipe Neto, Whindersson, Casimiro, Gaules, Coreano Doido |
| VIRAL-06    | 12.1-01     | GeminiWebTrendsAgent requests 25 topics per fetch                                               | SATISFIED    | GEMINI_TRENDS_MAX_TOPICS=25; agent reads constant from config            |
| VIRAL-07    | 12.1-01     | BlueSkyAgent uses specific BR humor keywords                                                    | SATISFIED    | _KEYWORDS_BR = 10 specific terms including "tio do pave", "zoeira"      |
| VIRAL-08    | 12.1-02     | Google Trends traffic parsing uses regex with K/M/B suffix support                              | SATISFIED    | `_parse_traffic()` with `re.search` and multipliers dict in google_trends.py |
| VIRAL-09    | 12.1-02     | Trend scores apply temporal velocity decay (e^(-age_hours/24))                                 | SATISFIED    | `_apply_temporal_decay()` in aggregator.py uses `math.exp(-age_hours/24)` |
| VIRAL-10    | 12.1-02     | TrendAggregator applies multi-source boost (1 + 0.2*(sources-1), capped 2.0x)                 | SATISFIED    | `source_counts` tracking + boost formula in aggregator.py               |
| VIRAL-11    | 12.1-02     | Reddit posts use engagement/position-based scoring instead of fixed 0.4                        | SATISFIED    | Two-pass approach: upvote extraction + position-based fallback           |
| VIRAL-12    | 12.1-03     | Curator uses LLM-based theme mapping instead of rigid KEYWORD_MAP                              | SATISFIED    | `_llm_map_theme()` is primary; KEYWORD_MAP preserved as fallback         |
| VIRAL-13    | 12.1-03     | Curator processes 20+ trends and produces 10-15 WorkOrders per run                             | SATISFIED    | broker.drain(max_items=30); curator_count=max(images_per_run*3, 15); default count=15 |
| VIRAL-14    | 12.1-03     | Curator filters topics with meme potential below 3 (LLM-rated 1-5)                            | SATISFIED    | Filter in `_selections_to_topics()`; ANALYZER_PROMPT includes meme_potential 1-5 |
| VIRAL-15    | 12.1-04     | Generated phrases validated for length (<120 chars), language, and format before use           | SATISFIED    | `_validate_phrase()` checks length, PT markers, English ratio, placeholder format + retry |
| VIRAL-16    | 12.1-04     | Topic-image coherence checked before generation, with LLM-based theme remapping on mismatch   | SATISFIED    | `_check_coherence()` + `_remap_theme()` in generation_layer.py; obvious_matches shortcut + LLM fallback |

**Note on REQUIREMENTS.md state:** The file contains an unresolved git merge conflict (markers `<<<<<<<`, `=======`, `>>>>>>>` at lines 37-49 and 158-170). The conflict shows two conflicting views of VIRAL-12 through VIRAL-16 status (one view marks 12-14 as pending, another marks 15-16 as pending). The actual implementation in the codebase confirms **all 16 requirements are satisfied** — the conflict is a documentation artifact, not a code issue.

---

### Anti-Patterns Found

| File                                          | Pattern                         | Severity | Impact                                                                                   |
|-----------------------------------------------|---------------------------------|----------|------------------------------------------------------------------------------------------|
| `.planning/REQUIREMENTS.md`                   | Unresolved git merge conflict   | Warning  | Non-blocking — documentation only; does not affect runtime behavior                     |
| `config.py` lines 104-116                     | Legacy PIPELINE_REDDIT_SUBREDDITS and PIPELINE_RSS_FEEDS constants retained | Info | Marked as "legacy" in comments; agents correctly ignore them; no runtime impact |

No blocking anti-patterns found. The legacy config constants are intentionally preserved (noted in SUMMARY) in case other code references them. The git conflict in REQUIREMENTS.md is a documentation issue only.

---

### Human Verification Required

#### 1. Full Pipeline Throughput Test

**Test:** Run `python -m src.pipeline_cli --mode once` with GOOGLE_API_KEY set and observe logs
**Expected:** Log shows 30 trends drained, 10+ WorkOrders emitted, meme_potential filter discards 0-5 topics, curator completes successfully
**Why human:** Requires live Gemini API + RSS feeds; cannot verify offline

#### 2. LLM Theme Remap Trigger

**Test:** Manually create a WorkOrder with `gandalf_topic="cafe com leite"` and `situacao_key="confronto"`, run through GenerationLayer
**Expected:** Logs show "Coherence mismatch: 'cafe com leite' vs theme 'confronto', remapping..." then "Remapped: confronto -> cafe"
**Why human:** Requires running pipeline and observing log output; LLM behavior varies

---

### Gaps Summary

No gaps found. All 16 requirements (VIRAL-01 through VIRAL-16) are implemented and verified in the codebase across 4 plans executed across 2 waves:

- **Wave 1 (Plans 01 + 02):** Agent cleanup, BR-first scoring, traffic parsing fix, temporal decay, multi-source boost
- **Wave 2 (Plans 03 + 04):** LLM theme mapping, meme potential filter, curator throughput increase, phrase validation, coherence check

The only outstanding item is the unresolved git merge conflict in `.planning/REQUIREMENTS.md` — the conflict markers should be resolved to reflect the correct final state (all 16 requirements completed).

---

_Verified: 2026-03-26T18:00:00Z_
_Verifier: Claude (gsd-verifier)_
