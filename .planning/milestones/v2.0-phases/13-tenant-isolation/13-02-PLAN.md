---
phase: 13-tenant-isolation
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - src/database/repositories/pipeline_repo.py
  - src/database/repositories/content_repo.py
  - src/database/repositories/job_repo.py
  - src/database/repositories/theme_repo.py
  - src/database/repositories/schedule_repo.py
autonomous: true
requirements:
  - TENANT-01
  - TENANT-02
  - TENANT-03

must_haves:
  truths:
    - "PipelineRunRepository filters runs by user via Character join"
    - "ContentPackageRepository filters packages by user via Character join"
    - "GeneratedImageRepository filters images by user via Character join"
    - "BatchJobRepository filters jobs by user via Character join"
    - "ScheduledPostRepository filters posts by user via Character join"
    - "ThemeRepository returns global + user's own themes"
    - "Admin user bypasses all repo filters"
    - "Single-resource lookups raise PermissionError for wrong user"
  artifacts:
    - path: "src/database/repositories/pipeline_repo.py"
      provides: "Tenant-filtered pipeline queries"
      contains: "PermissionError"
    - path: "src/database/repositories/content_repo.py"
      provides: "Tenant-filtered content queries"
      contains: "Character.user_id"
    - path: "src/database/repositories/job_repo.py"
      provides: "Tenant-filtered job queries"
      contains: "Character.user_id"
    - path: "src/database/repositories/theme_repo.py"
      provides: "Global + user theme queries"
      contains: "user_id"
    - path: "src/database/repositories/schedule_repo.py"
      provides: "Tenant-filtered schedule queries"
      contains: "Character.user_id"
  key_links:
    - from: "src/database/repositories/pipeline_repo.py"
      to: "src/database/models.py"
      via: "Join PipelineRun -> Character for user_id filter"
      pattern: "join.*Character.*PipelineRun\\.character_id"
    - from: "src/database/repositories/content_repo.py"
      to: "src/database/models.py"
      via: "Join ContentPackage -> Character for user_id filter"
      pattern: "join.*Character"
---

<objective>
Add tenant filtering to all remaining repositories (5 files) following the CharacterRepository pattern from Plan 01. Each repo gets user-aware read methods with admin bypass and PermissionError for cross-user single-resource access.

Purpose: Completes TENANT-02 (all repos filter by user_id) and TENANT-03 (admin bypass) at the data layer. Routes (Plan 03) will wire these updated repos.
Output: 5 updated repository files with tenant-aware query methods.
</objective>

<execution_context>
@C:\Users\VIP\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\VIP\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-tenant-isolation/13-CONTEXT.md
@.planning/phases/13-tenant-isolation/13-RESEARCH.md
@.planning/phases/13-tenant-isolation/13-01-SUMMARY.md

<interfaces>
<!-- CharacterRepository pattern (reference from Plan 01) -->
From src/database/repositories/character_repo.py (after Plan 01):
```python
class CharacterRepository:
    # List operation: query filter (user sees only own data)
    async def list_all(self, user=None, include_deleted=False):
        stmt = select(Character).order_by(Character.created_at.desc())
        if user and user.role != "admin":
            stmt = stmt.where(Character.user_id == user.id)
        ...

    # Single lookup: fetch-then-check (404 vs 403)
    async def get_by_slug(self, slug, user=None, include_deleted=False):
        ...fetch character...
        if user and user.role != "admin" and character.user_id != user.id:
            raise PermissionError("forbidden")
        return character
```

<!-- Models for join pattern -->
From src/database/models.py:
```python
class Character(Base):
    id, slug, user_id (NOT NULL after migration)

class PipelineRun(Base):
    character_id: Mapped[int]  # NOT NULL after migration
    character: Mapped[Optional["Character"]] = relationship(...)

class ContentPackage(Base):
    character_id: Mapped[Optional[int]]  # FK to characters.id

class GeneratedImage(Base):
    character_id: Mapped[Optional[int]]  # FK to characters.id

class BatchJob(Base):
    character_id: Mapped[Optional[int]]  # FK to characters.id

class ScheduledPost(Base):
    character_id: Mapped[Optional[int]]  # FK to characters.id

class Theme(Base):
    character_id: Mapped[Optional[int]]  # FK to characters.id
    user_id: Mapped[Optional[int]]       # NEW in Plan 01 migration
```

From src/database/models.py (User):
```python
class User(Base):
    role: Mapped[str]  # "admin" or "user"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PipelineRunRepository + ContentPackageRepository + GeneratedImageRepository tenant filtering</name>
  <files>src/database/repositories/pipeline_repo.py, src/database/repositories/content_repo.py</files>
  <read_first>
    - src/database/repositories/pipeline_repo.py (full file — all method signatures)
    - src/database/repositories/content_repo.py (full file — ContentPackageRepository + GeneratedImageRepository)
    - src/database/repositories/character_repo.py (reference pattern from Plan 01)
    - src/database/models.py (Character, PipelineRun, ContentPackage, GeneratedImage imports and relationships)
  </read_first>
  <action>
**Pattern for child tables (transitive ownership via Character join, per D-01):**
- Import `User` and `Character` from `src.database.models`
- List operations: `.join(Character, ChildTable.character_id == Character.id).where(Character.user_id == user.id)` when `user and user.role != "admin"`
- Single lookups (fetch-then-check for D-04): Fetch resource, then load its character via `await self.session.get(Character, resource.character_id)`, check `character.user_id != user.id` -> raise `PermissionError("forbidden")`
- When `user is None`: skip filtering (internal/system operations like background pipeline tasks, per Pitfall 2)
- When `user.role == "admin"`: skip filtering (D-07)

**PipelineRunRepository changes:**
- `get_by_run_id(self, run_id, user=None)`: Fetch run, if found and user provided and not admin, load Character via `self.session.get(Character, run.character_id)`, check ownership, raise `PermissionError("forbidden")` if mismatch.
- `get_by_run_id_with_relations(self, run_id, user=None)`: Same fetch-then-check pattern. The run already has character_id.
- `list_runs(self, limit, offset, status, character_id, user=None)`: Add join filter. `if user and user.role != "admin": stmt = stmt.join(Character, PipelineRun.character_id == Character.id).where(Character.user_id == user.id)`
- `count_runs(self, status, character_id, user=None)`: Same join filter for counting.
- Write methods (`create_run`, `update_run`, `finish_run`): NO changes — character_id is validated upstream by route using get_user_character.
- TrendEvent/WorkOrder/AgentStat methods: NO changes — accessed via pipeline_run_id which is already validated upstream.

**ContentPackageRepository changes:**
- `get_by_id(self, package_id, user=None)`: Fetch-then-check via Character join. Load package, get Character, check user_id.
- `list_packages(self, ..., user=None)`: Join filter via Character.
- `count(self, ..., user=None)`: Join filter via Character.
- `get_by_ids(self, ids, user=None)`: Join filter via Character.
- `get_by_id_with_character(self, package_id, user=None)`: Already loads character — add ownership check.
- `get_for_run(...)` and `get_recent_topics(...)`: NO changes (internal operations).

**GeneratedImageRepository changes:**
- `get_by_id(self, image_id, user=None)`: Fetch-then-check via Character.
- `list_images(self, ..., user=None)`: Join filter via Character.
- `count(self, ..., user=None)`: Join filter via Character.
  </action>
  <verify>
    <automated>cd C:/Users/VIP/testeDev/clip-flow && python -c "
from src.database.repositories.pipeline_repo import PipelineRunRepository
from src.database.repositories.content_repo import ContentPackageRepository, GeneratedImageRepository
import inspect
# Check user param exists on key methods
assert 'user' in inspect.signature(PipelineRunRepository.get_by_run_id).parameters
assert 'user' in inspect.signature(PipelineRunRepository.list_runs).parameters
assert 'user' in inspect.signature(ContentPackageRepository.get_by_id).parameters
assert 'user' in inspect.signature(GeneratedImageRepository.list_images).parameters
print('Pipeline+Content+Image repos OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - pipeline_repo.py contains `from src.database.models import PipelineRun, TrendEvent, WorkOrder, AgentStat, Character, User` (or equivalent import including Character and User)
    - pipeline_repo.py get_by_run_id signature has `user=None` parameter
    - pipeline_repo.py list_runs contains `.join(Character` for tenant filtering
    - pipeline_repo.py contains `raise PermissionError("forbidden")`
    - content_repo.py ContentPackageRepository.get_by_id has `user=None` parameter
    - content_repo.py list_packages contains `.join(Character` for tenant filtering
    - content_repo.py GeneratedImageRepository.get_by_id has `user=None` parameter
    - content_repo.py GeneratedImageRepository.list_images contains join filter
    - content_repo.py contains `raise PermissionError("forbidden")`
    - create_run, update_run, finish_run do NOT have user parameter (internal ops)
  </acceptance_criteria>
  <done>PipelineRunRepository, ContentPackageRepository, and GeneratedImageRepository all have user-aware methods with admin bypass and PermissionError for cross-user access.</done>
</task>

<task type="auto">
  <name>Task 2: BatchJobRepository + ThemeRepository + ScheduledPostRepository tenant filtering</name>
  <files>src/database/repositories/job_repo.py, src/database/repositories/theme_repo.py, src/database/repositories/schedule_repo.py</files>
  <read_first>
    - src/database/repositories/job_repo.py (full file)
    - src/database/repositories/theme_repo.py (full file)
    - src/database/repositories/schedule_repo.py (full file)
    - src/database/repositories/character_repo.py (reference pattern)
    - src/database/models.py (BatchJob, Theme, ScheduledPost, Character relationships)
  </read_first>
  <action>
**BatchJobRepository** (same transitive pattern as Task 1):
- `get_by_job_id(self, job_id, user=None)`: Fetch-then-check via Character. Load job, get Character by job.character_id, check user_id, raise PermissionError if mismatch.
- `list_jobs(self, ..., user=None)`: Join filter `.join(Character, BatchJob.character_id == Character.id).where(Character.user_id == user.id)` when user is not admin.
- `count(self, ..., user=None)`: Same join filter.
- Import `Character, User` from models.

**ThemeRepository** (special: global + user themes per D-03, Pitfall 4):
- Import `User` from models.
- `get_by_id(self, theme_id, user=None)`: Fetch theme. If theme has no user_id (global) or no character_id -> accessible to all. If theme.user_id is set and user is not admin and theme.user_id != user.id -> raise PermissionError("forbidden"). If theme.character_id is set, check via Character.user_id.
- `get_by_key(self, key, user=None)`: Same ownership check after fetch.
- `list_effective(self, character_id=None, user=None)`: Return union of:
  (a) Global themes: `Theme.user_id.is_(None) & Theme.character_id.is_(None) & Theme.is_deleted == False`
  (b) User's own themes: `Theme.user_id == user.id` (when user not admin)
  (c) Character themes: `Theme.character_id == character_id` (if character_id provided and character ownership validated upstream)
  For admin: return all non-deleted themes.
- `create(self, data)`: No signature change — route sets `data["user_id"] = current_user.id` for user themes.
- `list_global()`, `list_for_character()`: NO changes (global is public, character validated upstream).

**ScheduledPostRepository** (transitive pattern):
- `get_by_id(self, post_id, user=None)`: Fetch-then-check via Character.
- `list_posts(self, ..., user=None)`: Join filter via Character.
- `count(self, ..., user=None)`: Join filter via Character.
- `get_posts_by_date_range(self, start, end, user=None)`: Join filter via Character.
- `get_queue_summary(self, user=None)`: Join filter via Character.
- `get_due_posts(...)`: NO change (system scheduler operation).
- Import `Character, User` from models.
  </action>
  <verify>
    <automated>cd C:/Users/VIP/testeDev/clip-flow && python -c "
from src.database.repositories.job_repo import BatchJobRepository
from src.database.repositories.theme_repo import ThemeRepository
from src.database.repositories.schedule_repo import ScheduledPostRepository
import inspect
assert 'user' in inspect.signature(BatchJobRepository.get_by_job_id).parameters
assert 'user' in inspect.signature(ThemeRepository.get_by_id).parameters
assert 'user' in inspect.signature(ScheduledPostRepository.get_by_id).parameters
print('Job+Theme+Schedule repos OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - job_repo.py contains import for `Character` and `User`
    - job_repo.py get_by_job_id has `user=None` parameter
    - job_repo.py list_jobs contains `.join(Character` for tenant filtering
    - job_repo.py contains `raise PermissionError("forbidden")`
    - theme_repo.py get_by_id has `user=None` parameter
    - theme_repo.py contains logic for global themes (`user_id.is_(None)`)
    - theme_repo.py contains `raise PermissionError("forbidden")`
    - schedule_repo.py get_by_id has `user=None` parameter
    - schedule_repo.py list_posts contains `.join(Character` for tenant filtering
    - schedule_repo.py get_due_posts does NOT have user parameter
    - schedule_repo.py contains `raise PermissionError("forbidden")`
  </acceptance_criteria>
  <done>All 5 remaining repositories have user-aware methods. Theme repo handles global + user themes per D-03. Admin bypass works across all repos.</done>
</task>

</tasks>

<verification>
- Python import + signature inspection for all 5 repos validates user parameter presence
- `python -m pytest tests/test_tenant.py -x` (from Plan 01 scaffold) should still import cleanly
</verification>

<success_criteria>
- All 7 repos (character + 5 here + usage already done) have user-aware query methods
- Transitive ownership via Character join implemented consistently
- Admin bypass in all repos
- PermissionError raised for cross-user single-resource access
- Internal/system operations (user=None) skip filtering
</success_criteria>

<output>
After completion, create `.planning/phases/13-tenant-isolation/13-02-SUMMARY.md`
</output>
