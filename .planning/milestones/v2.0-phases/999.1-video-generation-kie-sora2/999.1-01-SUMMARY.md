---
phase: 999.1-video-generation-kie-sora2
plan: 01
subsystem: database, api, infra
tags: [alembic, sqlalchemy, gcs, kie-ai, sora2, video]

# Dependency graph
requires:
  - phase: 13-tenant-isolation
    provides: "ContentPackage and Theme ORM models with tenant fields"
provides:
  - "6 video columns on ContentPackage (video_path, video_source, video_prompt_used, video_task_id, video_metadata, video_status)"
  - "video_prompt_notes column on Theme for prompt improvement feedback loop"
  - "Alembic migration 012 for video schema"
  - "VIDEO_ENABLED, KIE_API_KEY, VIDEO_MODEL, VIDEO_DURATION and 9 more config constants"
  - "GCSUploader class (upload_image, delete_blob) for Kie.ai public URLs"
  - "src/video_gen/ package for video generation module"
affects: [999.1-02, 999.1-03, video-generation, kie-ai-integration]

# Tech tracking
tech-stack:
  added: [google-cloud-storage]
  patterns: [lazy-init-gcs-client, signed-url-for-external-api, video-status-state-machine]

key-files:
  created:
    - src/database/migrations/versions/012_add_video_columns.py
    - src/video_gen/__init__.py
    - src/video_gen/gcs_uploader.py
  modified:
    - src/database/models.py
    - config.py
    - requirements.txt

key-decisions:
  - "Migration 012 chains from 011 (sequential pattern, ignoring orphan ee583b64523f branch)"
  - "All video columns nullable with no server_default (MySQL TEXT/JSON constraint)"
  - "GCSUploader uses lazy client init to avoid import-time credential failures"

patterns-established:
  - "Video status state machine: null -> generating -> success/failed"
  - "GCS signed URL pattern for external API image access"
  - "Feature flag VIDEO_ENABLED defaults to false (opt-in only per D-05)"

requirements-completed: [VID-01, VID-02, VID-09]

# Metrics
duration: 2min
completed: 2026-03-26
---

# Phase 999.1 Plan 01: Video Foundation Summary

**Alembic migration 012 with 6 video columns on ContentPackage, video_prompt_notes on Theme, 13 config constants for Kie.ai Sora 2, and GCS uploader service for public image URLs**

## Performance

- **Duration:** 2 min
- **Started:** 2026-03-26T05:44:33Z
- **Completed:** 2026-03-26T05:47:02Z
- **Tasks:** 2
- **Files modified:** 6

## Accomplishments
- ContentPackage ORM extended with 6 video columns (video_path, video_source, video_prompt_used, video_task_id, video_metadata, video_status) plus idx_pkg_video_status index
- Theme ORM extended with video_prompt_notes for per-theme motion prompt improvement (per D-03)
- config.py has 13 new constants covering VIDEO_ENABLED, KIE_API_KEY, VIDEO_MODEL, VIDEO_DURATION, budget caps, polling config, and GCS settings
- GCSUploader class with upload_image() returning signed URLs and delete_blob() for cleanup

## Task Commits

Each task was committed atomically:

1. **Task 1: DB migration + ORM model updates for video columns** - `718bf91` (feat)
2. **Task 2: Config constants + GCS uploader service** - `b568e33` (feat)

**Plan metadata:** (pending final commit)

## Files Created/Modified
- `src/database/migrations/versions/012_add_video_columns.py` - Alembic migration adding video columns to content_packages and video_prompt_notes to themes
- `src/database/models.py` - Updated ContentPackage (6 video fields + index) and Theme (video_prompt_notes)
- `config.py` - 13 new VIDEO_* and GCS_* constants with env var loading
- `src/video_gen/__init__.py` - Package init for video generation module
- `src/video_gen/gcs_uploader.py` - GCSUploader class with upload_image and delete_blob
- `requirements.txt` - Added google-cloud-storage>=2.14.0

## Decisions Made
- Migration 012 chains from 011 following the sequential numbering pattern (ignoring orphan ee583b64523f branch which revises from 001)
- All video columns are nullable with no server_default to comply with MySQL TEXT/JSON constraints
- GCSUploader uses lazy client initialization to avoid import-time credential failures when GCS is not configured

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required at this stage. GCS credentials and KIE_API_KEY will be needed when VIDEO_ENABLED is set to true (Plans 02/03).

## Next Phase Readiness
- Database schema ready for video generation (migration 012 can be applied)
- Config constants ready for Kie.ai client (Plan 02) and video worker (Plan 03)
- GCS uploader ready to provide public URLs for Kie.ai API
- src/video_gen/ package exists for adding KieSora2Client (Plan 02) and MotionPromptGenerator (Plan 02)

## Self-Check: PASSED

- All 7 files verified present on disk
- Both task commits (718bf91, b568e33) verified in git log
- Python import verification passed for ORM models and config constants

---
*Phase: 999.1-video-generation-kie-sora2*
*Completed: 2026-03-26*
