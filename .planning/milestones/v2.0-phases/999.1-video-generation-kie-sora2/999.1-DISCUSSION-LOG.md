# Phase 999.1: Video Generation — Kie.ai Sora 2 - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-03-26
**Phase:** 999.1-video-generation-kie-sora2
**Areas discussed:** Video source image, Motion prompts, Public URL strategy, Pipeline integration, Sora 2 model tier, Video duration, Cost budget guardrails, Sora 2 character consistency

---

## Video Source Image

| Option | Description | Selected |
|--------|-------------|----------|
| Raw background (Recommended) | Send Gemini-generated background (no text, no watermark). Video stays clean — text added later by FFmpeg. | :heavy_check_mark: |
| Composed meme | Send final meme with phrase text + watermark baked in. Simpler but text may distort. | |
| Both options | Default raw background, allow composed meme per package. More flexibility. | |

**User's choice:** Raw background
**Notes:** Clean video for Phase 999.2 legend overlay.

---

## Motion Prompts

| Option | Description | Selected |
|--------|-------------|----------|
| LLM-generated (Recommended) | Gemini generates unique motion prompt from image metadata. ~$0.001/prompt. | :heavy_check_mark: |
| Static templates per theme | Pre-written templates per situacao_key. Predictable, zero cost. | |
| Hybrid: template + LLM refinement | Static base + Gemini adaptation. Best of both. | |

**User's choice:** LLM-generated
**Notes:** Each video gets a tailored prompt based on theme, pose, scene, phrase.

---

## Prompt Improvement

| Option | Description | Selected |
|--------|-------------|----------|
| Manual feedback loop (Recommended) | User rates videos, adds notes. video_prompt_notes field per theme accumulates learnings. | :heavy_check_mark: |
| Automatic scoring | Track engagement metrics from published Reels. Requires Phase 15 + Insights. | |
| Version history only | Store prompts + results. User manually picks best. | |

**User's choice:** Manual feedback loop
**Notes:** video_prompt_notes per theme, read by LLM when generating next prompt.

---

## Public URL Strategy

| Option | Description | Selected |
|--------|-------------|----------|
| Cloudflare R2 (Recommended) | Reuse Phase 14 CDN. Same bucket, same upload logic. | |
| GCS (Google Cloud Storage) | Use existing Google API credentials. Independent of Phase 14. | :heavy_check_mark: |
| FastAPI static serve + ngrok | Local file server + tunnel. Zero cost but fragile. | |

**User's choice:** GCS
**Notes:** Leverages existing Google credentials. Independent of Phase 14 CDN timeline.

---

## Pipeline Integration

| Option | Description | Selected |
|--------|-------------|----------|
| Opt-in per package (Recommended) | User selects which packages get video. No auto-generation. | :heavy_check_mark: |
| Batch trigger separately | Dedicated batch endpoint, decoupled from pipeline. | |
| Auto for every meme | Every pipeline run auto-generates videos. Highest cost. | |

**User's choice:** Opt-in per package
**Notes:** User controls cost. Triggered from gallery UI or API.

---

## Async Mode

| Option | Description | Selected |
|--------|-------------|----------|
| Background async (Recommended) | Triggers video, sets status='generating', user continues. Poll for result. | :heavy_check_mark: |
| Blocking wait | API waits until video done. Simpler but locks request 1-2 min. | |

**User's choice:** Background async
**Notes:** 30-120s generation time, poll via job system.

---

## Sora 2 Model Tier

| Option | Description | Selected |
|--------|-------------|----------|
| Standard (Recommended) | sora-2-image-to-video at $0.15/10s. 480-720p. | :heavy_check_mark: |
| Pro 720p | $0.45/10s. Higher detail. | |
| User selectable per video | Default Standard with tier dropdown. | |

**User's choice:** Standard
**Notes:** Sufficient for Instagram Reels (compressed to 720p anyway).

---

## Video Duration

| Option | Description | Selected |
|--------|-------------|----------|
| 10 seconds (Recommended) | Optimal for Reels. $0.15. | |
| 15 seconds | Longer animation. $0.225. | |
| User selectable (10 or 15) | Default 10s with toggle. | :heavy_check_mark: |

**User's choice:** User selectable (10 or 15)
**Notes:** Default 10s, simple toggle in UI/API.

---

## Cost Budget Guardrails

| Option | Description | Selected |
|--------|-------------|----------|
| Hard daily cap (Recommended) | VIDEO_DAILY_BUDGET_USD default $3. Block when reached. Show remaining. | :heavy_check_mark: |
| Soft warning + hard cap | Warning at 80%, block at 100%. | |
| Per-video cost display only | Show cost, no limits. User self-manages. | |

**User's choice:** Hard daily cap
**Notes:** $3/day default = ~20 videos/day.

---

## Sora 2 Character Consistency

| Option | Description | Selected |
|--------|-------------|----------|
| Yes, register characters (Recommended) | Register with character_id_list. Upload refs to Kie.ai. | :heavy_check_mark: |
| Skip for now | Rely on input image alone. | |
| Defer to future phase | Enhancement later. | |

**User's choice:** Register characters
**Notes:** Each character (Mago Mestre, future) registered with Kie.ai for cross-video consistency.

---

## Claude's Discretion

- Error handling and retry strategy
- GCS bucket config and lifecycle
- Gemini prompt template for motion prompts
- DB schema details
- Polling intervals and timeouts

## Deferred Ideas

- Auto-scoring via Instagram Insights (requires Phase 15)
- Pro tier upgrade per-video (future UI enhancement)
