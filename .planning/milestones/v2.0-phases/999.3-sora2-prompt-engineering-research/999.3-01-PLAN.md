---
phase: 999.3-sora2-prompt-engineering-research
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config.py
  - src/video_gen/video_prompt_builder.py
  - tests/test_video_prompt_builder.py
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05, D-07, D-08]

must_haves:
  truths:
    - "VIDEO_PROMPT_STYLE config constant exists with default 'v2'"
    - "All 17 themes have v2 templates with three-layer motion framework"
    - "v1 templates preserved unchanged for fallback"
    - "Version switching selects correct template set"
    - "v2 templates use present continuous tense throughout"
    - "v2 templates are 4-5 sentences each, 300-500 chars"
    - "Technique reference comments explain pattern choices"
  artifacts:
    - path: "config.py"
      provides: "VIDEO_PROMPT_STYLE env var constant"
      contains: "VIDEO_PROMPT_STYLE"
    - path: "src/video_gen/video_prompt_builder.py"
      provides: "v1/v2 template dicts, version switching, technique comments"
      contains: "MOTION_TEMPLATES_V2"
    - path: "tests/test_video_prompt_builder.py"
      provides: "Template validation tests"
      contains: "test_v2_all_17_themes"
  key_links:
    - from: "config.py"
      to: "src/video_gen/video_prompt_builder.py"
      via: "VIDEO_PROMPT_STYLE import"
      pattern: "VIDEO_PROMPT_STYLE"
    - from: "src/video_gen/video_prompt_builder.py"
      to: "src/video_gen/video_prompt_builder.py"
      via: "_get_templates() selects dict"
      pattern: "_get_templates"
---

<objective>
Add v2 motion templates with three-layer motion framework (camera/subject/physics) to VideoPromptBuilder, config-based version switching, and validation tests.

Purpose: Apply researched Sora 2 prompt engineering patterns from OpenAI Cookbook and awesome-sora2 community to improve video motion prompt quality. All 17 themes get rewritten templates using present continuous tense, explicit camera directions, and physics-aware material descriptions.

Output: Updated video_prompt_builder.py with dual template sets (v1/v2), config.py with VIDEO_PROMPT_STYLE, test file validating template structure.
</objective>

<execution_context>
@C:/Users/VIP/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/VIP/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/999.3-sora2-prompt-engineering-research/999.3-CONTEXT.md
@.planning/phases/999.3-sora2-prompt-engineering-research/999.3-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From config.py (VIDEO section, line ~344-401):
```python
# Existing pattern for VIDEO constants:
VIDEO_ENABLED = os.getenv("VIDEO_ENABLED", "false").lower() == "true"
VIDEO_DURATION = int(os.getenv("VIDEO_DURATION", "10"))
VIDEO_MODEL = os.getenv("VIDEO_MODEL", "sora-2-image-to-video")
VIDEO_LEGEND_ENABLED = os.getenv("VIDEO_LEGEND_ENABLED", "false").lower() == "true"
VIDEO_LEGEND_MODE = os.getenv("VIDEO_LEGEND_MODE", "static")
```

From src/video_gen/video_prompt_builder.py (current structure):
```python
MOTION_TEMPLATES: dict[str, str] = { ... }  # 17 themes, line 18-105
_SYSTEM_PROMPT = (...)   # line 110-127
_ENHANCE_PROMPT = (...)  # line 129-142

class VideoPromptBuilder:
    def build_motion_prompt(self, theme_key, phrase_context="", pose="", scene="", video_prompt_notes="") -> str
    def enhance_user_prompt(self, user_input, theme_key="") -> str
    def get_fallback_prompt(self, theme_key) -> str
    @staticmethod _build_safe_prompt(user_input) -> str
    @staticmethod _clean_llm_response(raw) -> str
```

Current 17 theme keys:
sabedoria, confusao, segunda_feira, vitoria, tecnologia, cafe, comida, trabalho,
relaxando, meditando, relacionamento, confronto, surpresa, internet, generico, cotidiano, descanso
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Config constant + test scaffold + v2 templates with three-layer motion framework</name>
  <files>config.py, src/video_gen/video_prompt_builder.py, tests/test_video_prompt_builder.py</files>

  <read_first>
    - config.py (lines 344-401 for VIDEO constants pattern)
    - src/video_gen/video_prompt_builder.py (entire file — 343 lines)
    - .planning/phases/999.3-sora2-prompt-engineering-research/999.3-RESEARCH.md (template examples, camera vocabulary, physics vocabulary)
  </read_first>

  <action>
**Step 1 — config.py**: Add `VIDEO_PROMPT_STYLE` constant after `VIDEO_LEGEND_FONT_SIZE` (line ~400), following the existing pattern. Per D-05, default to `"v2"`:

```python
# ===== Video Prompt Style — Phase 999.3 =====

# Prompt template version: "v1" (original) or "v2" (researched Sora 2 patterns)
VIDEO_PROMPT_STYLE = os.getenv("VIDEO_PROMPT_STYLE", "v2")
```

**Step 2 — video_prompt_builder.py**: Major restructuring of the module-level template dictionaries. Keep the existing code structure but:

a) Rename current `MOTION_TEMPLATES` to `MOTION_TEMPLATES_V1` (per D-02, D-05 — preserve v1 unchanged).

b) Create `MOTION_TEMPLATES_V2` dict with all 17 themes rewritten using the three-layer motion framework from RESEARCH.md. Per D-01 (i2v only), D-03 (camera + temporal + physics), D-04 (4-5 sentences each), D-08 (technique comments). Each template MUST:
   - Start with a camera direction (macro layer) — use THEME_CAMERA_MAP from research
   - Follow with primary subject action in present continuous tense (mid layer)
   - Include micro-actions (breathing, blinking, weight shifts)
   - End with physics/atmosphere (cloth behavior with material descriptors, particles, lighting)
   - Be 300-500 characters total
   - Use present continuous tense throughout ("is raising", "are drifting", "is swaying")

Templates to write (all 17):
- `sabedoria`: Slow push-in. Wizard is gently stroking beard, head nodding. Staff crystal pulsing. Robes swaying with breathing, particles drifting upward, warm golden rim light.
- `confusao`: Static camera with subtle instability. Wizard is tilting head side to side, eyebrows furrowing. Hand is scratching under hat. Staff wobbling, small sparkles popping, robe shifting with weight changes.
- `segunda_feira`: Very slow push-in. Wizard is raising coffee goblet with both hands, sipping slowly. Eyes are drooping then snapping open. Steam is curling upward with golden sparkles, heavy robe fabric draping over tired shoulders.
- `vitoria`: Slow dolly forward. Wizard is thrusting staff upward triumphantly. Body is leaning back, robes billowing outward dramatically. Golden particles erupting, fabric whipping with gesture force.
- `tecnologia`: Static camera with glow-lit parallax. Wizard is leaning forward poking glowing crystal. Head is tilting with widening eyes. Digital runes are flickering, hat fabric shifting, cool blue light dancing.
- `cafe`: Slow push-in. Wizard is cradling ornate mug, bringing close to inhale steam. Eyes are closing contentedly. Warm steam curling with golden sparkles, robe sleeves draping over wrists, soft warm rim light.
- `comida`: Gentle dolly forward. Wizard is stirring bubbling cauldron with large ladle, leaning in to smell. Free hand is gesturing approvingly. Colorful vapors swirling upward, heavy robe swaying with animated movements.
- `trabalho`: Static with desk parallax. Wizard is hunching over parchment, quill moving rapidly. Pausing to look up thinking. Ink trails transforming into floating runes, robe sleeve brushing parchment edge.
- `relaxando`: Static with gentle parallax. Wizard is reclining in chair, hat slowly tipping over eyes. Chest is rising and falling with deep slow breaths. Staff glowing faintly nearby, fingers twitching in lap, soft ambient particles.
- `meditando`: Static locked-off with subtle depth shift. Wizard is sitting cross-legged, chest rising slowly with deep breathing. Robes settling with each exhale. Blue-gold aura pulsing rhythmically outward, particles ascending.
- `relacionamento`: Very slow push-in. Wizard is holding glowing heart crystal, turning gently. Looking at it with knowing smile. Pink aura flowing between hands, robe fabric shifting with gentle arm movements.
- `confronto`: Subtle push-in, low angle. Wizard is planting staff firmly, both hands gripping tight. Stepping forward, robes whipping in dramatic wind. Golden energy crackling from staff tip, heavy fabric billowing with force.
- `surpresa`: Static with slight shake. Wizard is jolting backward, eyes going wide, hat flying up briefly. Hands are shooting up in shock. Sparkles bursting, robe fabric swinging with sudden movement.
- `internet`: Static with screen-glow parallax. Wizard is peering into glowing crystal ball, leaning in close. Expression shifting from curiosity to amusement. Blue light dancing on face, beard catching reflected glow.
- `generico`: Slow push-in. Wizard is shifting weight, adjusting staff grip. Looking around slowly, beard swaying. Crystal tip pulsing with rhythmic glow, robes flowing gently, ambient particles drifting.
- `cotidiano`: Static with gentle ambient drift. Wizard is leaning casually on staff, crossing one ankle. Raising ale mug for relaxed sip. Body swaying slightly, faint sparkles drifting, robe settling with casual posture.
- `descanso`: Static with very slow push-in. Wizard is sleeping in wooden chair, head nodding forward. Chest is rising and falling with slow breaths. Hat drooping over eyes, soft blue particles floating peacefully, hand twitching in lap.

c) Add a `_get_templates()` function that reads `VIDEO_PROMPT_STYLE` from config (with fallback chain: config module -> env var -> "v2" default) and returns the corresponding dict. Per D-05:

```python
def _get_templates() -> dict[str, str]:
    """Return active template set based on VIDEO_PROMPT_STYLE config.

    Per D-05: v1 returns original templates, v2 returns researched patterns.
    Default: v2 (Sora 2 prompt engineering from OpenAI Cookbook + awesome-sora2).
    """
    try:
        from config import VIDEO_PROMPT_STYLE
        style = VIDEO_PROMPT_STYLE
    except (ImportError, AttributeError):
        style = os.getenv("VIDEO_PROMPT_STYLE", "v2")

    if style == "v1":
        return MOTION_TEMPLATES_V1
    return MOTION_TEMPLATES_V2
```

d) Add `import os` at the top of the module (needed for env var fallback).

e) Update `build_motion_prompt()` to call `_get_templates()` instead of directly referencing `MOTION_TEMPLATES`. Change line:
```python
base_motion = MOTION_TEMPLATES.get(theme_key, MOTION_TEMPLATES["generico"])
```
to:
```python
templates = _get_templates()
base_motion = templates.get(theme_key, templates["generico"])
```

f) Update `enhance_user_prompt()` similarly — replace `MOTION_TEMPLATES.get(...)` with `_get_templates().get(...)`.

g) Update `get_fallback_prompt()` similarly — replace `MOTION_TEMPLATES.get(...)` with `_get_templates().get(...)`.

h) Keep backward compatibility: Add `MOTION_TEMPLATES = MOTION_TEMPLATES_V2` alias after the two dicts, so any external code still importing `MOTION_TEMPLATES` gets v2 by default.

i) Per D-08 — Add technique reference comments above each major section:
- Above MOTION_TEMPLATES_V1: `# Original v1 templates — preserved for fallback (per D-05)`
- Above MOTION_TEMPLATES_V2: `# v2 templates — Three-layer motion framework (OpenAI Cookbook + awesome-sora2)`
- Above each template: brief comment noting the camera choice rationale
- Above `_get_templates`: `# Per D-05: Version switching via VIDEO_PROMPT_STYLE env var`

j) Update the prompt cap comment on the `if len(prompt) > 500:` line. Change from:
`# Cap at 500 chars (Sora 2 allows 10k but short prompts work better)`
to:
`# Per D-07: Cap at 500 chars (research: 300-500 sweet spot for i2v)`

**Step 3 — tests/test_video_prompt_builder.py**: Create test file validating template structure:

```python
"""Tests for VideoPromptBuilder v2 templates and version switching.

Validates research-based template structure per Phase 999.3 decisions:
- D-02: All 17 themes present in v2
- D-03: Three-layer motion (camera + subject + physics)
- D-04: 4-5 sentences, 300-500 chars per template
- D-05: VIDEO_PROMPT_STYLE version switching
- D-07: 500 char prompt cap
"""
```

Tests to implement:
1. `test_v2_all_17_themes` — Assert MOTION_TEMPLATES_V2 has exactly 17 keys, matching MOTION_TEMPLATES_V1 keys.
2. `test_v1_preserved_unchanged` — Assert MOTION_TEMPLATES_V1 has all 17 keys and values match original content (spot-check 3 themes: sabedoria, cafe, generico — check they start with "The wizard").
3. `test_v2_template_length` — For each v2 template: assert 300 <= len(template) <= 500.
4. `test_v2_template_sentence_count` — For each v2 template: assert 4 <= sentence_count <= 6 (count periods followed by space or end of string).
5. `test_v2_present_continuous_tense` — For each v2 template: assert at least one occurrence of "is " or "are " (present continuous indicator).
6. `test_v2_three_layers` — For each v2 template: assert contains a camera-related word (from set: "camera", "push-in", "dolly", "pan", "static", "parallax", "shot", "angle"), a physics/material word (from set: "robe", "fabric", "beard", "staff", "particles", "glow", "light"), and a present continuous verb.
7. `test_version_switching_v1` — Monkeypatch `VIDEO_PROMPT_STYLE` to "v1" in config module, call `_get_templates()`, assert result is MOTION_TEMPLATES_V1.
8. `test_version_switching_v2` — Monkeypatch `VIDEO_PROMPT_STYLE` to "v2", call `_get_templates()`, assert result is MOTION_TEMPLATES_V2.
9. `test_version_switching_default` — Remove VIDEO_PROMPT_STYLE from env and config, verify `_get_templates()` returns MOTION_TEMPLATES_V2 (v2 is default per D-05).
10. `test_backward_compat_alias` — Assert `MOTION_TEMPLATES` exists and equals `MOTION_TEMPLATES_V2`.
11. `test_prompt_cap_500` — Assert `VideoPromptBuilder().get_fallback_prompt("generico")` produces output <= 600 chars (fallback adds camera instruction).
12. `test_config_constant_exists` — `from config import VIDEO_PROMPT_STYLE; assert VIDEO_PROMPT_STYLE in ("v1", "v2")`.

Use pytest with `monkeypatch` fixture for config/env manipulation. No external dependencies needed.
  </action>

  <verify>
    <automated>cd C:/Users/VIP/testeDev/clip-flow && python -m pytest tests/test_video_prompt_builder.py -x -v 2>&1 | tail -30</automated>
  </verify>

  <acceptance_criteria>
    - grep -c "VIDEO_PROMPT_STYLE" config.py returns >= 1
    - grep -c "MOTION_TEMPLATES_V1" src/video_gen/video_prompt_builder.py returns >= 1
    - grep -c "MOTION_TEMPLATES_V2" src/video_gen/video_prompt_builder.py returns >= 1
    - grep -c "_get_templates" src/video_gen/video_prompt_builder.py returns >= 3
    - grep -c "is " src/video_gen/video_prompt_builder.py returns >= 30 (present continuous in v2 templates)
    - grep -c "Three-layer\|OpenAI Cookbook\|awesome-sora2\|Per D-" src/video_gen/video_prompt_builder.py returns >= 5 (technique comments per D-08)
    - python -m pytest tests/test_video_prompt_builder.py -x exits 0
    - All 17 theme keys present in both MOTION_TEMPLATES_V1 and MOTION_TEMPLATES_V2
  </acceptance_criteria>

  <done>
    - config.py has VIDEO_PROMPT_STYLE constant defaulting to "v2"
    - video_prompt_builder.py has MOTION_TEMPLATES_V1 (original 17 themes unchanged) and MOTION_TEMPLATES_V2 (17 themes rewritten with three-layer framework, present continuous tense, 300-500 chars each)
    - _get_templates() function reads config and returns correct dict
    - build_motion_prompt(), enhance_user_prompt(), get_fallback_prompt() all use _get_templates()
    - MOTION_TEMPLATES backward-compat alias points to V2
    - Technique reference comments present (per D-08)
    - All 12 tests pass
  </done>
</task>

</tasks>

<verification>
```bash
# All tests pass
cd C:/Users/VIP/testeDev/clip-flow && python -m pytest tests/test_video_prompt_builder.py -x -v

# Config constant exists
python -c "from config import VIDEO_PROMPT_STYLE; print(f'VIDEO_PROMPT_STYLE={VIDEO_PROMPT_STYLE}')"

# Both template dicts exist with 17 keys each
python -c "from src.video_gen.video_prompt_builder import MOTION_TEMPLATES_V1, MOTION_TEMPLATES_V2; print(f'v1={len(MOTION_TEMPLATES_V1)} v2={len(MOTION_TEMPLATES_V2)}')"

# Version switching works
python -c "
import os; os.environ['VIDEO_PROMPT_STYLE'] = 'v1'
from src.video_gen.video_prompt_builder import _get_templates, MOTION_TEMPLATES_V1
assert _get_templates() is MOTION_TEMPLATES_V1
print('v1 switching OK')
"
```
</verification>

<success_criteria>
- VIDEO_PROMPT_STYLE in config.py defaults to "v2"
- 17 v2 templates all 300-500 chars, 4-5 sentences, present continuous tense, three-layer motion
- v1 templates preserved byte-for-byte from original
- _get_templates() version switching works
- 12 tests all green
- Technique reference comments present per D-08
</success_criteria>

<output>
After completion, create `.planning/phases/999.3-sora2-prompt-engineering-research/999.3-01-SUMMARY.md`
</output>
