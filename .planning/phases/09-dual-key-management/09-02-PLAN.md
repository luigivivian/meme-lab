---
phase: 09-dual-key-management
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/image_gen/gemini_client.py
  - src/api/routes/generation.py
autonomous: true
requirements: [QUOT-04, QUOT-05]

must_haves:
  truths:
    - "GeminiImageClient uses selector-resolved key instead of global singleton for image generation"
    - "GeminiImageClient works without selector (backward compat for notebook/CLI)"
    - "Generation API route passes force_tier to selector (admin only)"
    - "Non-admin force_tier param is silently ignored"
  artifacts:
    - path: "src/image_gen/gemini_client.py"
      provides: "api_key parameter on generation methods, _get_image_client cache"
      contains: "_get_image_client"
    - path: "src/api/routes/generation.py"
      provides: "force_tier query param with admin check, selector wiring"
      contains: "force_tier"
  key_links:
    - from: "src/api/routes/generation.py"
      to: "src/services/key_selector.py"
      via: "UsageAwareKeySelector().resolve()"
      pattern: "UsageAwareKeySelector"
    - from: "src/api/routes/generation.py"
      to: "src/api/deps.py"
      via: "Depends(get_current_user) for admin check"
      pattern: "get_current_user"
---

<objective>
Wire UsageAwareKeySelector into GeminiImageClient and generation API routes so image generation automatically uses the correct Gemini API key based on usage.

Purpose: Connects the selector logic (Plan 01) to the actual image generation pipeline.
Output: Modified GeminiImageClient with optional api_key injection, updated generation routes with force_tier admin param.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-dual-key-management/09-CONTEXT.md
@.planning/phases/09-dual-key-management/09-RESEARCH.md
@.planning/phases/09-dual-key-management/09-01-SUMMARY.md

<interfaces>
From src/services/key_selector.py (Plan 01 output):

  @dataclass(frozen=True)
  class KeyResolution:
      api_key: str
      tier: str   # "gemini_free" or "gemini_paid"
      mode: str   # "auto", "forced_env", "forced_request", "free_only"

  class UsageAwareKeySelector:
      def __init__(self): ...  # reads env vars
      async def resolve(self, user_id: int, session: AsyncSession, force_tier: str | None = None) -> KeyResolution

From src/image_gen/gemini_client.py (current):
  - Line 18: from src.llm_client import _get_client
  - Line 750: _get_client() in is_available()
  - Line 760: client = _get_client() in _tentar_gerar()
  - All generation methods are SYNC. Selector resolve() is ASYNC.
  - Resolution must happen BEFORE sync calls (in the async route handler).

From src/api/routes/generation.py (current):
  - /single is sync def, /compose is async def, /refine is sync def
  - No auth dependencies currently
  - Imports: from src.api.deps import resolver_tema, output_dir

From src/api/deps.py:
  async def get_current_user(authorization=Header(...), session=Depends(db_session)) -> User
  async def db_session() -> AsyncSession
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add api_key parameter to GeminiImageClient generation methods</name>
  <files>src/image_gen/gemini_client.py</files>
  <read_first>
    - src/image_gen/gemini_client.py (full file to find ALL _get_client() and _tentar_gerar call sites)
    - src/llm_client.py (lines 35-45 to understand _get_client() singleton)
    - src/services/key_selector.py (KeyResolution fields)
  </read_first>
  <action>
    Modify GeminiImageClient to optionally accept a resolved API key on generation methods.
    The key comes as a plain string from KeyResolution.api_key. When None, falls back to _get_client().

    1. Add _get_image_client method (after __init__, before _load_referencias):

        def _get_image_client(self, api_key: str):
            """Get or create a genai Client for the given API key.
            Caches clients by key (max 2: free + paid)."""
            if not hasattr(self, "_client_cache"):
                self._client_cache = {}
            if api_key not in self._client_cache:
                from google import genai
                self._client_cache[api_key] = genai.Client(api_key=api_key)
            return self._client_cache[api_key]

    2. Modify _tentar_gerar: add api_key: str | None = None as last param.
       Replace "client = _get_client()" with:
       "client = self._get_image_client(api_key) if api_key else _get_client()"

    3. Modify generate_image: add api_key: str | None = None as last param.
       Pass api_key=api_key to every self._tentar_gerar() call inside generate_image.
       There are multiple calls (primary model + fallback loop). ALL must get the param.

    4. Modify refine_image: add api_key: str | None = None. Pass to _tentar_gerar calls.

    5. Modify generate_with_refinement: add api_key: str | None = None.
       Pass to generate_image and refine_image calls.

    6. Search for remaining self._tentar_gerar( calls without api_key. Fix any missed.

    DO NOT modify:
    - is_available() — still uses _get_client() directly
    - _get_client import at line 18
    - discover_image_models() standalone function
    - __init__ constructor signature
  </action>
  <verify>
    <automated>cd C:/Users/VIP/testeDev/clip-flow &amp;&amp; python -c "from src.image_gen.gemini_client import GeminiImageClient; import inspect; assert 'api_key' in inspect.signature(GeminiImageClient._tentar_gerar).parameters; assert 'api_key' in inspect.signature(GeminiImageClient.generate_image).parameters; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - src/image_gen/gemini_client.py contains "def _get_image_client(self, api_key"
    - src/image_gen/gemini_client.py contains "def _tentar_gerar(self, modelo: str, partes: list, temperatura: float, api_key"
    - src/image_gen/gemini_client.py contains "self._get_image_client(api_key) if api_key else _get_client()"
    - src/image_gen/gemini_client.py contains "def generate_image(" with api_key parameter
    - src/image_gen/gemini_client.py contains "def generate_with_refinement(" with api_key parameter
    - src/image_gen/gemini_client.py still contains "from src.llm_client import _get_client" (backward compat preserved)
    - is_available() still calls _get_client() without api_key (unchanged)
  </acceptance_criteria>
  <done>All GeminiImageClient generation methods accept optional api_key param. Default behavior (None) unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Wire selector into generation routes with force_tier admin param</name>
  <files>src/api/routes/generation.py</files>
  <read_first>
    - src/api/routes/generation.py (current routes to modify)
    - src/api/deps.py (get_current_user, db_session dependencies)
    - src/services/key_selector.py (UsageAwareKeySelector API)
    - src/database/repositories/usage_repo.py (UsageRepository.increment for post-generation logging)
  </read_first>
  <action>
    Update generation routes to resolve the API key via UsageAwareKeySelector before calling GeminiImageClient, and add force_tier query param (admin-only per D-08).

    1. Add imports at top of generation.py:

        from fastapi import APIRouter, HTTPException, Depends, Query
        from sqlalchemy.ext.asyncio import AsyncSession
        from src.api.deps import resolver_tema, output_dir, get_current_user, db_session
        from src.services.key_selector import UsageAwareKeySelector
        from src.database.repositories.usage_repo import UsageRepository

    2. Convert /single from sync to async (needed for await selector.resolve()):

        @router.post("/single", summary="Gera uma imagem individual")
        async def generate_single(
            req: SingleRequest,
            force_tier: str | None = Query(None, pattern="^(free|paid)$"),
            current_user=Depends(get_current_user),
            session: AsyncSession = Depends(db_session),
        ):

    3. Add selector resolution at start of generate_single:

        # D-08: admin-only force_tier (D-09: priority order)
        effective_force = force_tier if (force_tier and current_user.role == "admin") else None

        selector = UsageAwareKeySelector()
        resolution = await selector.resolve(
            user_id=current_user.id,
            session=session,
            force_tier=effective_force,
        )

    4. Pass resolution.api_key to GeminiImageClient methods:
       - client.generate_image(..., api_key=resolution.api_key)
       - client.generate_with_refinement(..., api_key=resolution.api_key)  (for auto_refine path)

    5. After successful generation, increment usage with correct tier:

        if path:
            repo = UsageRepository(session)
            await repo.increment(
                user_id=current_user.id,
                service="gemini_image",
                tier=resolution.tier.replace("gemini_", ""),  # "free" or "paid"
            )
            await session.commit()

    6. Wrap the sync client calls in asyncio.to_thread() since the route is now async:
       - result = await asyncio.to_thread(client.generate_image, ..., api_key=resolution.api_key)
       - For generate_with_refinement similarly

    7. Update /compose similarly:
       - Add force_tier, current_user, session params
       - Resolve key before image generation
       - Pass api_key to generate_image / generate_with_refinement calls
       - Increment usage after success

    8. Update /refine similarly:
       - Convert to async
       - Add force_tier, current_user, session params
       - Resolve key, pass api_key, increment usage

    9. Add import asyncio at top if not present.

    IMPORTANT: Return the resolution.tier in the response dict as "tier": resolution.tier
    so the frontend/caller knows which key was used. Example:
        return { "success": True, ..., "tier": resolution.tier, "key_mode": resolution.mode }
  </action>
  <verify>
    <automated>cd C:/Users/VIP/testeDev/clip-flow &amp;&amp; python -c "from src.api.routes.generation import router; routes = [r.path for r in router.routes]; print(routes); assert '/single' in routes; print('OK')" &amp;&amp; grep -c "force_tier" src/api/routes/generation.py &amp;&amp; grep -c "UsageAwareKeySelector" src/api/routes/generation.py &amp;&amp; grep -c "get_current_user" src/api/routes/generation.py</automated>
  </verify>
  <acceptance_criteria>
    - src/api/routes/generation.py contains "from src.services.key_selector import UsageAwareKeySelector"
    - src/api/routes/generation.py contains "from src.database.repositories.usage_repo import UsageRepository"
    - src/api/routes/generation.py contains "force_tier: str | None = Query(None"
    - src/api/routes/generation.py contains "current_user=Depends(get_current_user)"
    - src/api/routes/generation.py contains "session: AsyncSession = Depends(db_session)"
    - src/api/routes/generation.py contains "selector = UsageAwareKeySelector()"
    - src/api/routes/generation.py contains "await selector.resolve("
    - src/api/routes/generation.py contains "api_key=resolution.api_key"
    - src/api/routes/generation.py contains "current_user.role ==" (admin check for force_tier per D-08)
    - src/api/routes/generation.py contains "repo.increment(" (usage logging after generation)
    - All 3 routes (/single, /refine, /compose) are async def
  </acceptance_criteria>
  <done>All generation routes resolve the API key via selector, pass it to GeminiImageClient, log usage with correct tier, and support admin-only force_tier param.</done>
</task>

</tasks>

<verification>
```bash
# Selector tests still pass
python -m pytest tests/test_key_selector.py -x -v

# GeminiImageClient imports cleanly with new params
python -c "from src.image_gen.gemini_client import GeminiImageClient; print('OK')"

# Generation routes import cleanly
python -c "from src.api.routes.generation import router; print('OK')"

# Existing tests not broken
python -m pytest tests/test_atomic_counter.py -x

# force_tier and selector are wired in generation routes
grep "UsageAwareKeySelector" src/api/routes/generation.py
grep "force_tier" src/api/routes/generation.py
grep "api_key=resolution" src/api/routes/generation.py
```
</verification>

<success_criteria>
- GeminiImageClient._tentar_gerar accepts optional api_key param (QUOT-04)
- GeminiImageClient falls back to _get_client() when api_key is None (backward compat)
- Generation routes resolve key via UsageAwareKeySelector before calling client (QUOT-05)
- force_tier query param exists on /single, /compose, /refine (D-08)
- Non-admin force_tier is silently ignored (D-08)
- Usage is incremented with correct tier after successful generation
- Response includes tier and key_mode for caller visibility
</success_criteria>

<output>
After completion, create `.planning/phases/09-dual-key-management/09-02-SUMMARY.md`
</output>
