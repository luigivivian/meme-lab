---
phase: 999.1-video-generation-kie-sora2
verified: 2026-03-26T06:30:00Z
status: passed
score: 10/10 must-haves verified
re_verification: false
gaps: []
human_verification:
  - test: "Trigger POST /generate/video with a real KIE_API_KEY and GCS credentials set"
    expected: "Video status transitions from null -> generating -> success, MP4 file downloaded locally"
    why_human: "Requires live Kie.ai credits and a configured GCS bucket — cannot mock the polling loop"
  - test: "Trigger POST /generate/video twice on the same package without waiting for completion"
    expected: "Second call returns HTTP 409 Conflict"
    why_human: "Requires a running FastAPI server to test race condition guard"
  - test: "Exhaust VIDEO_DAILY_BUDGET_USD by triggering enough video generations in a single day"
    expected: "Subsequent request returns HTTP 429 with message 'Daily video budget exhausted'"
    why_human: "Requires multiple real API calls to accumulate cost entries in api_usage table"
---

# Phase 999.1: Video Generation — Kie.ai Sora 2 Verification Report

**Phase Goal:** Convert generated meme images into 10-15 second portrait videos using Kie.ai Sora 2 image-to-video API, with LLM-generated motion prompts that improve per-theme over time via manual feedback. GCS for public image URLs, daily budget cap, opt-in per content package.
**Verified:** 2026-03-26T06:30:00Z
**Status:** PASSED
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | ContentPackage model has video_path, video_source, video_prompt_used, video_task_id, video_metadata, video_status columns | VERIFIED | `src/database/models.py` lines 342-347, all 6 `Mapped[Optional[...]]` fields present |
| 2 | Theme model has video_prompt_notes column for per-theme motion prompt learnings | VERIFIED | `src/database/models.py` line 150, `video_prompt_notes: Mapped[Optional[str]]` |
| 3 | config.py exposes VIDEO_ENABLED, VIDEO_DURATION, VIDEO_MODEL, VIDEO_DAILY_BUDGET_USD, KIE_API_KEY, GCS_BUCKET_NAME | VERIFIED | config.py lines 347-381, all 13 VIDEO_* and GCS_* constants with env var loading |
| 4 | GCS uploader can upload a local file and return a signed URL | VERIFIED | `src/video_gen/gcs_uploader.py`: `class GCSUploader` with `upload_image()` returning `generate_signed_url()` result and `delete_blob()` |
| 5 | KieSora2Client can create a video task from an image URL and poll for results | VERIFIED | `src/video_gen/kie_client.py`: 5 async methods — create_task, get_task_status, poll_until_complete, download_video, generate_video |
| 6 | KieSora2Client supports character_id_list for visual consistency per D-10 | VERIFIED | kie_client.py line 149: `"character_id_list": character_ids or []` in create_task payload |
| 7 | VideoPromptBuilder generates LLM motion prompts via Gemini per D-02 | VERIFIED | `src/video_gen/video_prompt_builder.py`: `build_motion_prompt()` calls `from src.llm_client import generate` with `tier="lite"` |
| 8 | VideoPromptBuilder reads video_prompt_notes from themes for per-theme improvement per D-03 | VERIFIED | video_prompt_builder.py line 132-167: `video_prompt_notes` param incorporated into LLM user_message |
| 9 | User can trigger video generation for an approved content package via POST /generate/video | VERIFIED | `src/api/routes/video.py` line 221: `@router.post("")` with `approval_status == "approved"` check (line 262) |
| 10 | Daily budget cap blocks video generation when VIDEO_DAILY_BUDGET_USD is exceeded per D-09 | VERIFIED | video.py `_check_budget()` calls `repo.get_daily_cost(user_id, "kie_video")` and raises HTTP 429 if exceeded |

**Score:** 10/10 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/database/migrations/versions/012_add_video_columns.py` | Alembic migration adding video columns to content_packages and video_prompt_notes to themes | VERIFIED | `revision='012'`, `down_revision='011'`, 7 `op.add_column` calls + index, correct `downgrade()` |
| `src/database/models.py` | Updated ContentPackage and Theme ORM with video fields | VERIFIED | All 6 video fields on ContentPackage, `video_prompt_notes` on Theme, `Index("idx_pkg_video_status")` in `__table_args__` |
| `config.py` | Video generation config constants | VERIFIED | 13 constants: VIDEO_ENABLED, KIE_API_KEY, VIDEO_DURATION, VIDEO_MODEL, VIDEO_DAILY_BUDGET_USD, VIDEO_COST_PER_SECOND, KIE_MAX_CONCURRENT, KIE_POLL_*, GCS_BUCKET_NAME, GCS_SIGNED_URL_EXPIRY, GENERATED_VIDEOS_DIR |
| `src/video_gen/__init__.py` | Package init for video generation module | VERIFIED | File exists with module docstring |
| `src/video_gen/gcs_uploader.py` | GCS upload + signed URL service | VERIFIED | `class GCSUploader` with `upload_image()`, `delete_blob()`, lazy client init, `generate_signed_url()` |
| `src/video_gen/kie_client.py` | Async Kie.ai Sora 2 API client | VERIFIED | `class KieSora2Client`, `@dataclass VideoGenerationResult` (10 fields), `class KieAPIError`, all 5 required async methods |
| `src/video_gen/video_prompt_builder.py` | LLM-powered motion prompt generator with per-theme notes | VERIFIED | `class VideoPromptBuilder`, `MOTION_TEMPLATES` with 17 keys, `build_motion_prompt()`, `get_fallback_prompt()` |
| `src/api/routes/video.py` | Video generation API routes | VERIFIED | `router = APIRouter(prefix="/generate/video")`, 4 endpoints (POST, POST /batch, GET /status/{id}, GET /budget) |
| `src/api/models.py` | Pydantic request/response models for video endpoints | VERIFIED | `VideoGenerateRequest`, `VideoBatchRequest`, `VideoStatusResponse`, `VideoBudgetResponse` all present |
| `src/api/app.py` | Updated app with video route registration | VERIFIED | Line 24: `from src.api.routes import ... video`; line 98: `app.include_router(video.router)` |
| `src/database/repositories/usage_repo.py` | get_daily_cost and kie_video service registration | VERIFIED | `get_daily_cost()` at line 299, `("kie_video", "standard")` in both `_DEFAULT_LIMITS` (line 29) and `_KNOWN_SERVICES` (line 36) |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `012_add_video_columns.py` | `src/database/models.py` | Migration columns match ORM model fields | WIRED | Migration adds `video_path`, `video_source`, `video_prompt_used`, `video_task_id`, `video_metadata`, `video_status` to `content_packages`; models.py has identical 6 fields |
| `config.py` | `src/video_gen/gcs_uploader.py` | `GCS_BUCKET_NAME` env var | WIRED | gcs_uploader.py line 13: `from config import GCS_BUCKET_NAME, GCS_SIGNED_URL_EXPIRY` |
| `src/video_gen/kie_client.py` | `https://api.kie.ai/api/v1/jobs/createTask` | `httpx.AsyncClient POST` | WIRED | kie_client.py line 18: `import httpx`; line 159-163: `await client.post(f"{self.BASE_URL}/jobs/createTask")` |
| `src/video_gen/video_prompt_builder.py` | `src/llm_client.py` | `generate()` call for LLM motion prompts | WIRED | video_prompt_builder.py line 154: `from src.llm_client import generate`; line 172-177: called with `tier="lite"` |
| `src/api/routes/video.py` | `src/video_gen/kie_client.py` | `KieSora2Client.generate_video()` | WIRED | video.py line 92: `from src.video_gen.kie_client import KieSora2Client`; line 155-162: `client.generate_video(image_url=signed_url, ...)` |
| `src/api/routes/video.py` | `src/video_gen/gcs_uploader.py` | `GCSUploader.upload_image()` | WIRED | video.py line 93: `from src.video_gen.gcs_uploader import GCSUploader`; line 150-152: `uploader.upload_image(bg_path, blob_name)` |
| `src/api/routes/video.py` | `src/video_gen/video_prompt_builder.py` | `VideoPromptBuilder.build_motion_prompt()` | WIRED | video.py line 94: `from src.video_gen.video_prompt_builder import VideoPromptBuilder`; line 131-136: `prompt_builder.build_motion_prompt(theme_key=..., video_prompt_notes=theme_notes)` |
| `src/api/routes/video.py` | `src/database/repositories/usage_repo.py` | `UsageRepository.get_daily_cost()` for D-09 budget | WIRED | video.py lines 62-66: `repo.get_daily_cost(user_id, "kie_video")`; lines 179-185: `repo.increment(service="kie_video", ...)` |
| `src/api/routes/video.py` | `src/database/models.py` | `ContentPackage.video_status` updates | WIRED | video.py lines 145, 166, 193, 211, 279, 381: `pkg.video_status` set to "failed", "success", "generating" |
| `src/api/app.py` | `src/api/routes/video.py` | `app.include_router(video.router)` | WIRED | app.py line 24: import; line 98: `app.include_router(video.router)` |

---

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `src/api/routes/video.py` (budget endpoint) | `spent_today` | `UsageRepository.get_daily_cost()` -> `SELECT SUM(cost_usd) FROM api_usage WHERE user_id=? AND service='kie_video' AND date=today` | Yes — real DB query with `func.sum()` | FLOWING |
| `src/api/routes/video.py` (status endpoint) | `pkg.video_status`, `pkg.video_path`, etc. | `SELECT FROM content_packages WHERE id=?` -> ContentPackage ORM fields | Yes — real DB row with video columns | FLOWING |
| `src/video_gen/kie_client.py` (generate_video) | `video_url` | Kie.ai API response -> `json.loads(data["resultJson"])` -> `resultUrls[0]` | Yes — parses actual API response | FLOWING (runtime dependency on external API) |
| `src/video_gen/video_prompt_builder.py` | Motion prompt | `generate()` (LLM) using MOTION_TEMPLATES as context + theme metadata | Yes — real LLM call with fallback to static template | FLOWING |

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| MOTION_TEMPLATES has 17 keys covering all themes | `python -c "from src.video_gen.video_prompt_builder import MOTION_TEMPLATES; print(len(MOTION_TEMPLATES))"` | 17 | PASS |
| VideoGenerationResult dataclass importable with correct fields | Module structure inspection | All 10 fields present: video_url, local_path, task_id, prompt_used, model, duration_seconds, cost_usd, generation_time_ms, character_ids, source_image_url | PASS |
| Video routes registered in app | `grep "video" src/api/app.py` | Import + `include_router(video.router)` at lines 24 and 98 | PASS |
| All 6 commits from summaries verified in git log | `git log --oneline 718bf91 b568e33 99f4853 dc42a5a 662241d 4805a03` | All 6 commits verified as feat(999.1-0x) entries | PASS |
| get_daily_cost queries real DB | `grep -A 15 "async def get_daily_cost" usage_repo.py` | `SELECT SUM(cost_usd) FROM api_usage WHERE user_id=? AND service=? AND date=today AND status='success'` | PASS |
| Live Kie.ai video generation and polling | Requires runtime with KIE_API_KEY + GCS credentials | Cannot test without live credentials | SKIP |

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|---------|
| VID-01 | 999.1-01-PLAN.md | ContentPackage table has video columns (video_path, video_source, video_prompt_used, video_task_id, video_metadata, video_status) | SATISFIED | models.py lines 342-347; migration 012 lines 26-50 |
| VID-02 | 999.1-01-PLAN.md | Theme table has video_prompt_notes for per-theme motion prompt improvement | SATISFIED | models.py line 150; migration 012 lines 53-56 |
| VID-03 | 999.1-02-PLAN.md | KieSora2Client can create video tasks, poll status, and download results via Kie.ai API | SATISFIED | kie_client.py: `create_task()`, `poll_until_complete()`, `download_video()`, `generate_video()` — all implemented |
| VID-04 | 999.1-02-PLAN.md | VideoPromptBuilder generates unique LLM motion prompts per video via Gemini | SATISFIED | video_prompt_builder.py: `build_motion_prompt()` calls `llm_client.generate()` with tier="lite" |
| VID-05 | 999.1-02-PLAN.md | Motion prompts incorporate per-theme video_prompt_notes for iterative quality improvement | SATISFIED | video_prompt_builder.py lines 166-167: `if video_prompt_notes: parts.append(f"Improvement notes: {video_prompt_notes}")` |
| VID-06 | 999.1-03-PLAN.md | User can trigger video generation for approved content packages via POST /generate/video | SATISFIED | video.py line 221: `@router.post("")`; D-05 opt-in enforced at line 262 |
| VID-07 | 999.1-03-PLAN.md | User can check video status and view completed videos via GET /generate/video/status/{id} | SATISFIED | video.py line 407: `@router.get("/status/{content_package_id}")` returning VideoStatusResponse |
| VID-08 | 999.1-03-PLAN.md | User can generate videos in batch via POST /generate/video/batch | SATISFIED | video.py line 301: `@router.post("/batch")` with full ownership and budget checks |
| VID-09 | 999.1-01-PLAN.md, 999.1-03-PLAN.md | Daily video budget cap enforced via VIDEO_DAILY_BUDGET_USD with cost tracking in api_usage | SATISFIED | `_check_budget()` (D-09 guard); `get_daily_cost()` in usage_repo.py; `repo.increment(service="kie_video")` after success |
| VID-10 | 999.1-02-PLAN.md | Character consistency via Kie.ai character_id_list registration | SATISFIED | kie_client.py line 149: `"character_id_list": character_ids or []`; video.py line 287: `character_ids=req.character_ids` passed to background task |

**All 10 requirements satisfied. No orphaned requirements.**

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| None | — | — | — | — |

No TODO, FIXME, placeholder, or stub patterns found in any of the 7 new video module files. All implementations are substantive with real API calls, real DB queries, and real fallback logic.

---

### Human Verification Required

#### 1. End-to-end video generation with live credentials

**Test:** Set `VIDEO_ENABLED=true`, `KIE_API_KEY=<real key>`, `GOOGLE_APPLICATION_CREDENTIALS=<service account JSON>`, `GCS_BUCKET_NAME=<real bucket>`. Approve a content package and call `POST /generate/video` with `{"content_package_id": N, "duration": 10}`.
**Expected:** Response returns immediately with `video_status="generating"`. After 30-120 seconds, `GET /generate/video/status/N` returns `video_status="success"` with `video_path` pointing to a downloaded MP4.
**Why human:** Requires live Kie.ai API credits and a configured GCS bucket with proper IAM permissions. Cannot validate polling loop or actual video generation programmatically.

#### 2. HTTP 409 duplicate generation guard

**Test:** Start a video generation on a package, then immediately issue a second `POST /generate/video` for the same package ID before the first completes.
**Expected:** Second request returns HTTP 409 `"Video generation already in progress"`.
**Why human:** Requires a running FastAPI server to test the `video_status == "generating"` check under concurrent conditions.

#### 3. Daily budget enforcement via real api_usage accumulation

**Test:** With `VIDEO_DAILY_BUDGET_USD=0.30` (2 videos), generate 2 videos successfully (each 10s at $0.15). Then request a third video.
**Expected:** Third request returns HTTP 429 `"Daily video budget exhausted. Spent $0.30 of $0.30"`.
**Why human:** Requires real api_usage rows inserted by actual completed video generations to accumulate daily cost.

---

### Gaps Summary

No gaps found. All 10 observable truths are verified, all 11 artifacts pass all 4 levels of verification (exists, substantive, wired, data-flows), all 10 key links are confirmed wired in the actual code, and all VID-01 through VID-10 requirements are satisfied with direct code evidence.

The phase is ready for production use pending external service configuration (KIE_API_KEY, GCS credentials) which are correctly documented as runtime requirements and safely gated behind `VIDEO_ENABLED=false` by default.

---

*Verified: 2026-03-26T06:30:00Z*
*Verifier: Claude (gsd-verifier)*
