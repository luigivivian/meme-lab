---
phase: 999.4-instagram-reels-pipeline
plan: 01
subsystem: database, api
tags: [sqlalchemy, pydantic, alembic, reels, gemini-tts]

# Dependency graph
requires: []
provides:
  - ReelsConfig and ReelsJob ORM models (src/database/models.py)
  - Alembic migration 017 creating reels_config and reels_jobs tables
  - Pydantic models for roteiro JSON schema and API request/response
  - REELS_* config constants with env var fallback
  - src/reels_pipeline/ package structure
affects: [999.4-02, 999.4-03, 999.4-04, 999.4-05]

# Tech tracking
tech-stack:
  added: []
  patterns: [reels pipeline config module with env var fallback, roteiro schema with cenas array]

key-files:
  created:
    - src/database/migrations/versions/017_reels_pipeline.py
    - src/reels_pipeline/__init__.py
    - src/reels_pipeline/config.py
    - src/reels_pipeline/models.py
  modified:
    - src/database/models.py
    - config.py

key-decisions:
  - "TTS/transcription defaults set to gemini (not openai) per CONTEXT.md D-02 update — zero new dependencies"
  - "TimestampMixin used for both ReelsConfig and ReelsJob (consistent with existing models)"
  - "server_default on all non-JSON columns for MySQL compatibility"

patterns-established:
  - "ReelsConfig per-user/character config pattern with UniqueConstraint(user_id, character_id, name)"
  - "RoteiroSchema with cenas[] array — standard JSON structure for all script generation"

requirements-completed: [REEL-08]

# Metrics
duration: 3min
completed: 2026-03-28
---

# Phase 999.4 Plan 01: Foundation Summary

**ReelsConfig/ReelsJob ORM models, Alembic migration 017, Pydantic roteiro schema, and REELS_* config constants with Gemini TTS defaults**

## Performance

- **Duration:** 3 min
- **Started:** 2026-03-28T18:04:02Z
- **Completed:** 2026-03-28T18:06:31Z
- **Tasks:** 2
- **Files modified:** 6

## Accomplishments
- ReelsConfig ORM model with 25+ configurable fields (images, script, TTS, video assembly, subtitles, branding)
- ReelsJob ORM model with full job lifecycle tracking (status, artifacts, publishing, cost)
- Migration 017 chaining from 016 with both tables, indexes, and unique constraint
- Pydantic models: RoteiroSchema, CenaSchema, ReelGenerateRequest, ReelStatusResponse, ReelJobResponse, ReelsConfigRequest, ReelsConfigResponse
- Config module with zero-config defaults using Gemini for TTS and transcription (no new dependencies)

## Task Commits

Each task was committed atomically:

1. **Task 1: DB migration + ORM models** - `a77cb75` (feat)
2. **Task 2: Config constants + Pydantic models** - `6f27068` (feat)

## Files Created/Modified
- `src/database/models.py` - Added ReelsConfig and ReelsJob ORM models (tables 15 and 16)
- `src/database/migrations/versions/017_reels_pipeline.py` - Creates reels_config and reels_jobs tables
- `src/reels_pipeline/__init__.py` - Package marker
- `src/reels_pipeline/config.py` - REELS_* constants with env var fallback
- `src/reels_pipeline/models.py` - Pydantic models for roteiro schema and API
- `config.py` - Added REELS_ENABLED feature flag

## Decisions Made
- TTS and transcription providers default to "gemini" (per updated CONTEXT.md D-02) instead of "openai" from the research phase — avoids adding openai Python dependency
- TTS voice default "Puck" (Gemini Flash TTS voice with good PT-BR quality)
- All non-JSON columns use server_default for MySQL compatibility (following existing codebase pattern)

## Deviations from Plan

None - plan executed exactly as written.

## Known Stubs

None - all models and config are complete data definitions, not stubs.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Database foundation ready for all subsequent pipeline modules
- Pydantic models ready for API route implementation
- Config constants ready for image gen, script gen, TTS, transcription, and video assembly modules

---
*Phase: 999.4-instagram-reels-pipeline*
*Completed: 2026-03-28*
