---
phase: 999.6-reels-pipeline-v2
plan: 01
subsystem: api
tags: [gemini, reels, pipeline, image-gen, script-gen]

requires:
  - phase: 999.4-instagram-reels-pipeline
    provides: "Reels pipeline with interactive step endpoints and image/script generation"
provides:
  - "Reordered STEP_ORDER: prompt -> script -> images -> tts -> srt -> video"
  - "Text-only script generation (image_paths optional)"
  - "Per-cena image generation using script context (generate_reel_images_per_cena)"
affects: [999.6-02, 999.6-03, reels-frontend]

tech-stack:
  added: []
  patterns: ["text-only script gen path for interactive pipeline", "per-cena image gen using cena narracao + legenda_overlay"]

key-files:
  created: []
  modified:
    - src/api/routes/reels.py
    - src/reels_pipeline/script_gen.py
    - src/reels_pipeline/image_gen.py
    - src/reels_pipeline/main.py

key-decisions:
  - "n_imagens defaults to 5 when image_paths=None (text-only mode)"
  - "Per-cena gen falls back to generic gen when no script available in step_state"

patterns-established:
  - "Text-only vs multimodal branching in generate_script based on image_paths presence"
  - "Per-cena image prompts include narracao + legenda_overlay for scene context"

requirements-completed: [REELV2-01, REELV2-02]

duration: 2min
completed: 2026-03-28
---

# Phase 999.6 Plan 01: Step Reorder + Per-Cena Image Generation Summary

**Reordered interactive pipeline steps so script generates before images, with per-cena context-aware image generation from approved script cenas**

## Performance

- **Duration:** 2 min
- **Started:** 2026-03-28T20:55:09Z
- **Completed:** 2026-03-28T20:57:19Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments
- STEP_ORDER reordered to prompt -> script -> images -> tts -> srt -> video
- Script generation supports text-only mode (image_paths=None) for interactive pipeline where script runs before images
- New generate_reel_images_per_cena generates 1 image per cena using narracao + legenda_overlay as context
- Images step handler reads approved script from step_state and routes to per-cena or generic generation

## Task Commits

Each task was committed atomically:

1. **Task 1: Reorder backend step order + text-only script generation** - `f175b9b` (feat)
2. **Task 2: Per-cena image generation using script context** - `7e4effb` (feat)

## Files Created/Modified
- `src/api/routes/reels.py` - Reordered STEP_ORDER, updated script step to pass image_paths=None, updated images step to use per-cena gen
- `src/reels_pipeline/script_gen.py` - Made image_paths optional, added text-only prompt path
- `src/reels_pipeline/image_gen.py` - Added generate_reel_images_per_cena function
- `src/reels_pipeline/main.py` - Added run_step_images_per_cena method, updated run_step_script signature

## Decisions Made
- Default n_imagens=5 when no images provided (text-only mode) to maintain consistent script structure
- Per-cena generation falls back to generic generation when no script is available in step_state (backward compatibility)
- Full pipeline run() method left unchanged — still passes images to script in the old order for non-interactive mode

## Deviations from Plan
None - plan executed exactly as written.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Backend step reorder complete, ready for frontend stepper update (Plan 02)
- Per-cena image gen ready, will be used by Hailuo video step (Plan 03)
- Sequential pipeline (run()) still works with old order for backward compatibility

---
*Phase: 999.6-reels-pipeline-v2*
*Completed: 2026-03-28*
