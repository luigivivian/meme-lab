---
phase: quick-260330-ie5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - memelab/src/components/ads/ad-presets.ts
  - memelab/src/components/ads/step-scene.tsx
  - memelab/src/components/ads/step-prompt.tsx
  - memelab/src/components/ads/step-analysis.tsx
autonomous: true
requirements: [QUICK-IE5]

must_haves:
  truths:
    - "Scene step shows background presets filtered by detected niche"
    - "Prompt step shows camera and lighting presets filtered by detected niche"
    - "Analysis step allows editing product_description and scene_suggestions inline"
    - "All presets still work when no niche is selected (show all options)"
  artifacts:
    - path: "memelab/src/components/ads/ad-presets.ts"
      provides: "Niche-organized preset constants for backgrounds, cameras, lighting"
    - path: "memelab/src/components/ads/step-scene.tsx"
      provides: "Scene step with niche-filtered background and lighting presets"
    - path: "memelab/src/components/ads/step-prompt.tsx"
      provides: "Prompt step with niche-filtered camera and lighting presets"
    - path: "memelab/src/components/ads/step-analysis.tsx"
      provides: "Analysis step with editable product_description and scene_suggestions"
  key_links:
    - from: "memelab/src/components/ads/step-scene.tsx"
      to: "memelab/src/components/ads/ad-presets.ts"
      via: "import NICHE_BACKGROUNDS, NICHE_SCENE_LIGHTS"
      pattern: "import.*from.*ad-presets"
    - from: "memelab/src/components/ads/step-prompt.tsx"
      to: "memelab/src/components/ads/ad-presets.ts"
      via: "import NICHE_CAMERAS, NICHE_LIGHTINGS"
      pattern: "import.*from.*ad-presets"
---

<objective>
Enhance the ads wizard pipeline with niche-organized presets and editable analysis fields.

Purpose: Current scene/prompt steps have flat, generic preset lists. By organizing backgrounds, cameras, and lighting by product niche (food, beauty, tech, moda, fitness), users get contextually relevant options. Making analysis results editable lets users refine AI suggestions before proceeding.

Output: Updated step-scene, step-prompt, step-analysis components + new ad-presets.ts shared constants file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@memelab/src/components/ads/step-scene.tsx
@memelab/src/components/ads/step-prompt.tsx
@memelab/src/components/ads/step-analysis.tsx
@memelab/src/components/ads/wizard.tsx (for NICHES constant and niche state flow)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create niche-organized preset data and update step-scene + step-prompt</name>
  <files>memelab/src/components/ads/ad-presets.ts, memelab/src/components/ads/step-scene.tsx, memelab/src/components/ads/step-prompt.tsx</files>
  <action>
Create `memelab/src/components/ads/ad-presets.ts` with niche-organized preset constants. Structure:

```typescript
type Preset = { value: string; label: string };
type NichePresets = Record<string, Preset[]>;  // key = niche value from wizard NICHES
```

**NICHE_BACKGROUNDS** — each niche gets 5-8 curated options + a "custom" entry:
- `food`: marmore-branco, mesa-madeira, superficie-chocolate, tecido-linho, cozinha-rustica, pedra-natural, custom
- `beauty`: tecido-suave, studio-clean, marmore-rosa, petala-blur, espelho-dourado, custom
- `tech`: gradiente-escuro, neon-accent, metal-escovado, circuito-abstrato, studio-minimal, holografico, custom
- `moda`: passarela-blur, tecido-neutro, espelho-fashion, urban-concreto, studio-ciclorama, custom
- `fitness`: academia-blur, natureza-verde, concreto-industrial, quadra-esportiva, studio-escuro, custom
- `outros`: marmore-branco, gradiente-escuro, mesa-madeira, concreto-minimal, studio-ciclorama, tecido-neutro, natureza-blur, custom

**NICHE_SCENE_LIGHTS** — each niche gets 4-6 options:
- `food`: natural-suave, golden-hour, zenital-difusa, backlit-halo
- `beauty`: ring-light, soft-fill, golden-hour, contraluz
- `tech`: rim-light-dramatica, neon-accent, low-key, studio-3pontos
- `moda`: lateral-dramatica, studio-3pontos, golden-hour, high-key
- `fitness`: natural-dura, dramatica-lateral, high-key, studio-rim
- `outros`: all 6 lights from current SCENE_LIGHTS

**NICHE_CAMERAS** — per niche (currently in step-prompt as flat CAMERA_MOVES):
- `food`: orbital, dolly-in, static, crane-up, steadicam
- `beauty`: dolly-in, orbital, tracking, static, steadicam
- `tech`: orbital, dolly-out, tracking, crane-up, handheld
- `moda`: tracking, steadicam, dolly-in, orbital, crane-up
- `fitness`: handheld, tracking, dolly-in, crane-up, steadicam
- `outros`: all 8 from current CAMERA_MOVES

**NICHE_LIGHTINGS** — per niche (currently in step-prompt as flat LIGHTING_SETUPS):
- `food`: soft-natural, golden-hour, high-key, backlit
- `beauty`: soft-natural, studio-3point, high-key, neon-accent
- `tech`: dramatic-rim, neon-accent, low-key, studio-3point
- `moda`: studio-3point, dramatic-rim, golden-hour, high-key
- `fitness`: dramatic-rim, high-key, soft-natural, backlit
- `outros`: all 8 from current LIGHTING_SETUPS

Export a helper: `getPresetsForNiche(presetMap: NichePresets, niche: string): Preset[]` — returns niche-specific presets, falls back to `outros` when niche is empty or unknown.

Also export the existing COMPOSITIONS and MOODS arrays unchanged (move from their current files so ad-presets.ts is the single source of truth for all preset data).

**Update step-scene.tsx:**
- Add `niche` to Props interface (passed from parent jobId page via analysis result)
- Remove inline BACKGROUNDS and SCENE_LIGHTS constants
- Import `NICHE_BACKGROUNDS`, `NICHE_SCENE_LIGHTS`, `COMPOSITIONS`, `getPresetsForNiche` from ad-presets
- In SceneConfig, compute `const backgrounds = getPresetsForNiche(NICHE_BACKGROUNDS, niche)` and use for the background Select
- Same for scene lights
- Keep the "custom" option logic for background (show Input when bg === "custom")
- Default selections: pick first non-custom value from the niche-filtered list

**Update step-prompt.tsx:**
- Add `niche` to Props interface
- Remove inline CAMERA_MOVES and LIGHTING_SETUPS constants
- Import `NICHE_CAMERAS`, `NICHE_LIGHTINGS`, `MOODS`, `getPresetsForNiche` from ad-presets
- In PromptConfig, compute filtered presets based on niche
- Keep the video action textarea, mood select, and background input as-is (those are not niche-specific)
  </action>
  <verify>
    <automated>cd /Users/luigivivian/meme-lab/memelab && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - ad-presets.ts exports niche-organized presets for backgrounds, scene lights, cameras, lightings, moods, compositions
    - step-scene.tsx accepts niche prop and filters background/light selects accordingly
    - step-prompt.tsx accepts niche prop and filters camera/lighting selects accordingly
    - Both fall back to full lists when niche is empty/unknown
    - TypeScript compiles with no errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Make analysis step editable and wire niche prop through stepper</name>
  <files>memelab/src/components/ads/step-analysis.tsx, memelab/src/components/ads/stepper.tsx</files>
  <action>
**Update step-analysis.tsx:**
- Replace the read-only `AnalysisResult` component with an editable version
- `product_description` field: render as a Textarea (not read-only div), allow user to edit. Store edits in local state initialized from `data.product_description`
- `scene_suggestions` field: if array, render each suggestion as a separate editable Input row with a small "x" to remove. Add a "+ Adicionar sugestao" button to append new entries. Store edits in local state initialized from `data.scene_suggestions`
- `niche`, `tone`, `audience` fields remain read-only display (these are already editable in the wizard and don't need inline editing here)
- Add an `onUpdate?: (updates: Record<string, unknown>) => void` prop. When user clicks "Confirmar Analise", call `onUpdate` with the edited `product_description` and `scene_suggestions` values so the parent can persist changes
- Import Textarea, Input, Button, Plus, X icons as needed
- Keep the existing approve/regenerate button flow — editing happens inline before approve

**Update stepper.tsx (or the parent jobId page):**
Read stepper.tsx first to understand how step components receive their props. The `niche` value comes from the analysis step result (`stepState.result.niche`). Wire it:
- Extract `niche` from the analysis step's result data
- Pass `niche` prop to StepScene and StepPrompt components
- If analysis step is not yet completed, pass empty string (components fall back to full lists)
  </action>
  <verify>
    <automated>cd /Users/luigivivian/meme-lab/memelab && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - Analysis step shows product_description as editable Textarea
    - Analysis step shows scene_suggestions as editable list with add/remove
    - niche, tone, audience remain read-only in analysis (already editable in wizard)
    - StepScene and StepPrompt receive niche prop from analysis result
    - TypeScript compiles clean
  </done>
</task>

</tasks>

<verification>
1. `cd memelab && npx tsc --noEmit` — zero errors
2. `cd memelab && npm run build` — builds successfully
3. Manual: navigate to /ads, create a new ad job, verify scene step shows niche-filtered backgrounds, prompt step shows niche-filtered cameras/lighting, analysis step fields are editable
</verification>

<success_criteria>
- ad-presets.ts contains 6 niche categories x 4 preset types (backgrounds, scene lights, cameras, lightings) + shared moods and compositions
- step-scene.tsx and step-prompt.tsx use niche-filtered presets with fallback to full lists
- step-analysis.tsx allows inline editing of product_description (textarea) and scene_suggestions (editable list)
- niche prop flows from analysis result through stepper to scene and prompt steps
- All TypeScript compiles, build succeeds
</success_criteria>

<output>
After completion, create `.planning/quick/260330-ie5-enhance-ads-wizard-scene-step-with-custo/260330-ie5-SUMMARY.md`
</output>
