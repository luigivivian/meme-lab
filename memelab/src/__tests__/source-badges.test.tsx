import { describe, it, expect } from "vitest";

describe("Source Badges", () => {
  it.todo("returns 'gemini_free' for gemini source with tier=free");
  it.todo("returns 'gemini_paid' for gemini source with tier=paid");
  it.todo("returns 'gemini' for gemini source without tier metadata (legacy)");
  it.todo("returns 'static' for static source");
  it.todo("returns 'comfyui' for comfyui source");
});
